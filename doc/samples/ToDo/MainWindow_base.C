/*
 * All files in this project are distributed under the GNU General
 * Public License. This is Free Software.
 */
// ---------------------------------------
// MainWindow_base.C
//
// This file was automaticly generated by
// View Designer/X v1.1.beta
//
// User: lazy@apfel.goetting5
// Date: Sun Mar  9 17:54:21 1997
// 
// Don't edit this file, please !
// ---------------------------------------


// Include class definition

#include "MainWindow_base.h"

// Include X11 header
#include <X11/xpm.h>

// Include OSF/Motif header files
#include <Xm/Frame.h>
#include <X11/Shell.h>
#include <Xm/Form.h>
#include <Xm/ScrolledW.h>
#include <Xm/List.h>
#include <Xm/RowColumn.h>
#include <Xm/PushB.h>
#include <Xm/Separator.h>








#define RES_CONVERT(t,s) cvtResourceFromString(parent, t, s)


// -----------------------------------
// MainWindow_base(parent,args,n)
// -----------------------------------

MainWindow_base::MainWindow_base(Widget p, ArgList f_args, Cardinal f_n)
{
  Widget parent;
  Arg args[100];
  Cardinal n = 0;

  if(p) 
    parent = p;
  else
    parent = getTopLevel();

  if(p) {
    MainWindow = XtCreateWidget("MainWindow",xmFrameWidgetClass,
				       parent,f_args,f_n);
  } else { 
   
    popupShell = MainWindow = XtCreatePopupShell("MainWindow", applicationShellWidgetClass,
			parent, args, n);
  }
  createWidgets();
  addCallbacks();
  addRelations();
}


// ----------------------------
// createWidgets()
// ----------------------------

void MainWindow_base::createWidgets()
{
  Widget parent;           // the parent for every widget
  Arg args[100];           // resource list
  Cardinal n;              // number of resources

  // Now we create every Widget

  
  /* ---------- Creating Widget:    form_1 -------------- */
  parent = MainWindow;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 330); n++;
  XtSetArg(args[n], "width", 400); n++;
  XtSetArg(args[n], "x", 260); n++;
  XtSetArg(args[n], "y", 130); n++;


  /* Creating Widget */
  form_1 = XtCreateWidget("form_1", xmFormWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(form_1);
  


  
  /* ---------- Creating Widget:    scrolledWindow_1 -------------- */
  parent = form_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 274); n++;
  XtSetArg(args[n], "width", 390); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "bottomAttachment", XmATTACH_WIDGET); n++;
  XtSetArg(args[n], "leftAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  scrolledWindow_1 = XtCreateWidget("scrolledWindow_1", xmScrolledWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(scrolledWindow_1);
  


  
  /* ---------- Creating Widget:    list -------------- */
  parent = scrolledWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "scrollBarDisplayPolicy", XmSTATIC); n++;


  /* Creating Widget */
  list = XtCreateWidget("list", xmListWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(list);
  


  
  /* ---------- Creating Widget:    rowColumn_1 -------------- */
  parent = form_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 31); n++;
  XtSetArg(args[n], "width", 390); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 270); n++;
  XtSetArg(args[n], "entryAlignment", XmALIGNMENT_CENTER); n++;
  XtSetArg(args[n], "marginHeight", 5); n++;
  XtSetArg(args[n], "marginWidth", 5); n++;
  XtSetArg(args[n], "orientation", XmHORIZONTAL); n++;
  XtSetArg(args[n], "packing", XmPACK_COLUMN); n++;
  XtSetArg(args[n], "bottomAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "bottomOffset", 5); n++;
  XtSetArg(args[n], "leftAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;


  /* Creating Widget */
  rowColumn_1 = XtCreateWidget("rowColumn_1", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_1);
  


  
  /* ---------- Creating Widget:    newButton -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  newButton = XtCreateWidget("newButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(newButton);
  


  
  /* ---------- Creating Widget:    detailButton -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  detailButton = XtCreateWidget("detailButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(detailButton);
  


  
  /* ---------- Creating Widget:    delButton -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  delButton = XtCreateWidget("delButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(delButton);
  


  
  /* ---------- Creating Widget:    helpButton -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  helpButton = XtCreateWidget("helpButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(helpButton);
  


  
  /* ---------- Creating Widget:    separator_1 -------------- */
  parent = form_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 10); n++;
  XtSetArg(args[n], "width", 398); n++;
  XtSetArg(args[n], "x", 1); n++;
  XtSetArg(args[n], "y", 284); n++;
  XtSetArg(args[n], "bottomAttachment", XmATTACH_WIDGET); n++;
  XtSetArg(args[n], "leftAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "rightAttachment", XmATTACH_FORM); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;


  /* Creating Widget */
  separator_1 = XtCreateWidget("separator_1", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_1);
  



}



// ----------------------------
// addCallbacks()
// ----------------------------

void MainWindow_base::addCallbacks()
{

  XtAddCallback( form_1, "helpCallback",
                 help_me_CB, this);

  XtAddCallback( list, "defaultActionCallback",
                 list_double_click_CB, this);

  XtAddCallback( newButton, "activateCallback",
                 new_item_CB, this);

  XtAddCallback( detailButton, "activateCallback",
                 show_item_CB, this);

  XtAddCallback( delButton, "activateCallback",
                 delete_item_CB, this);

  XtAddCallback( helpButton, "activateCallback",
                 help_me_CB, this);

}



// ----------------------------
// addRelations()
// ----------------------------

void MainWindow_base::addRelations()
{
  XtVaSetValues(scrolledWindow_1, "bottomWidget", separator_1, NULL);

  XtVaSetValues(separator_1, "bottomWidget", rowColumn_1, NULL);


}


// ----------------------------
// manageWidget()
// ----------------------------
void MainWindow_base::manageWidget(Widget, XtPointer c, XtPointer)
{
  XtManageChild((Widget)c);
}


// ----------------------------
// createPixmapFromData()
// ----------------------------

Pixmap MainWindow_base::createPixmapFromData(char **data)
{
  Pixmap pix, mask;
  XpmAttributes attr;

  memset(&attr, 0, sizeof(attr));
  attr.valuemask = XpmReturnPixels;

  if( !XpmCreatePixmapFromData(XtDisplay(VcView::getTopLevel()),
		               XRootWindowOfScreen(XtScreen(VcView::getTopLevel())),
			       data,
			       &pix, &mask, &attr) )
    return pix;
  return XmUNSPECIFIED_PIXMAP;
}

// ------------------------------------------
// createPixmapFromFile()
//
// Creating a pixmap by XpmReadFileToPixmap()
// Uses XFILESEARCHPATH to find the pixmap
// default path: /usr/lib/X11/pixmaps/%N.xpm
// ------------------------------------------

Pixmap MainWindow_base::createPixmapFromFile(char *name)
{
  String string;
  Pixmap pix, mask;
  XpmAttributes attr;

  string = XtResolvePathname(XtDisplay(VcView::getTopLevel()),
			     "pixmaps", name, "", NULL,
			     NULL, 0, NULL);
  if(string) {
    memset(&attr, 0, sizeof(attr));
    attr.valuemask = XpmReturnPixels;
    if( !XpmReadFileToPixmap(XtDisplay(VcView::getTopLevel()),
			     XRootWindowOfScreen(
			     XtScreen(VcView::getTopLevel())),
			     string,
			     &pix, &mask, &attr) )
      return pix;
    return XmUNSPECIFIED_PIXMAP;
    XtFree(string);
  }
  return XmUNSPECIFIED_PIXMAP;
}

// ---------------------------------------------------------
// Following class methods are used as a callback.
// The method is calling a abstract virtual method, which
// must be overloaded in the derived class 'MainWindow'
// ---------------------------------------------------------

void MainWindow_base::help_me_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->help_me(wg,callData);
}

void MainWindow_base::list_double_click_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->list_double_click(wg,callData);
}

void MainWindow_base::new_item_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->new_item(wg,callData);
}

void MainWindow_base::show_item_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->show_item(wg,callData);
}

void MainWindow_base::delete_item_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->delete_item(wg,callData);
}


