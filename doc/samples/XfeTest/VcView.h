/*
 * All files in this project are distributed under the GNU General
 * Public License. This is Free Software.
 */
// ----------------------------------------------------
// This file was generated by View Designer/X
// 
// Do not edit !
//
// ----------------------------------------------------

#ifndef _VcView_H_
#define _VcView_H_

#include <X11/Intrinsic.h>

class VcView {
protected:
  Widget popupShell;
  static Widget firstTopLevel;     // the first created toplevel
  static XtAppContext context;     // the application context

  static void freeMem(Widget wg, XtPointer clientData, XtPointer callData);
  static void popupMenuHandler(Widget wg, XtPointer clientData, 
			       XEvent *event, Boolean *cont);
  static void addPopupMenuHandler(Widget menu, Widget parent);

public:
  VcView() { popupShell = NULL; }
  ~VcView() { if(popupShell) XtDestroyWidget(popupShell); }

  void open();             // open a View
  void close();            // close a View

  // init Xt-Intrinsic, open the display 
  // and create the toplevel widget
  static Widget init(char *applClass, 
		     int &argc, char **argv, char *fbRes=NULL);

  // running the application main loop
  static void run();
  
  static Widget getTopLevel() { return firstTopLevel; }

  // Helpfull function
  static XtArgVal cvtResourceFromString(Widget wg, char *type, char *value);

};


#endif
