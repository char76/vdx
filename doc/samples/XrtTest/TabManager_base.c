/*
 * All files in this project are distributed under the GNU General
 * Public License. This is Free Software.
 */
/* -----------------------------------------
 * TabManager_base.c                           
 *                                          
 * This file was automaticly generated by   
 * View Designer/X v1.2.beta
 *                 
 * User: lazy@apfel.goetting5
 * Date: Sun Feb  8 18:49:30 1998
 *                         
 * Don't edit this file, please!            
 *                                          
 * -----------------------------------------
 */

/* Used in createWidgets() */

#define RES_CONVERT(t,s) cvtResourceFromString(parent, t, s)
  

/* Include the header files for Motif */

#include <stdlib.h>
#include <Xm/Frame.h>
#include <X11/Shell.h>
#include <Xm/Form.h>
#include <Xm/XrtTabM.h>
#include <Xm/XrtTabB.h>
#include <Xm/Xrt3d.h>
#include <Xm/XrtAligner.h>
#include <Xm/XrtField.h>
#include <Xm/XrtTable.h>






/* Include linked interfaces */




#include "TabManager_base.h"
#include "TabManager.h"
#include "vcmisc.h"


/* Forward declarations */

static void TabManager_createWidgets(
#if NeedFunctionPrototypes
	TabManager *self
#endif
);

static void TabManager_addCallbacks(
#if NeedFunctionPrototypes
	TabManager *self
#endif
);

static void TabManager_addRelations(
#if NeedFunctionPrototypes
	TabManager *self
#endif
);


/* ----------------------------------------- */
/* Forward declarations for methods          */
/* ----------------------------------------- */





/* ----------------------------------------- */
/* The initialization function of this       */
/* module                                    */
/* ----------------------------------------- */

TabManager *createTabManager_base(p, f_args, f_n)
	Widget p;
	ArgList f_args;
	Cardinal f_n;
{
	Widget parent;
	Arg args[100];
	Cardinal n = 0;
	TabManager *self;

	self = (TabManager*)XtMalloc(sizeof(TabManager));   	/* Allocate Memory */
	memset(self, 0, sizeof(TabManager));

	if(p)
		parent = p;
	else
		parent = getTopLevel();

	if(p) {                                 
		self->base.topWidget = self->base.TabManager = 
			XtCreateWidget("TabManager",xmFrameWidgetClass,
				       parent, f_args, f_n);
	} else {
	        

		self->base.topWidget = self->base.popupShell = self->base.TabManager = 
			XtCreatePopupShell("TabManager", topLevelShellWidgetClass,
			parent, args, n);
	}
	TabManager_createWidgets(self); 	/* create widgets               */
	TabManager_addCallbacks(self);	/* add callbacks                */
	TabManager_addRelations(self);	/* set resources of type widget */

	if(self->base.TabManager)		/* add callback to free memory  */
		XtAddCallback(self->base.TabManager, XmNdestroyCallback,
			      vcDestroyCallback, self);
	return self;
}



/* ----------------------------------------- */
/* Create widgets                            */
/* ----------------------------------------- */

static void TabManager_createWidgets(self)
TabManager *self;
{
  Widget parent;
  Arg args[100];
  Cardinal n;

  
  /* ---------- Creating Widget:    form -------------- */
  parent = self->base.TabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmFormWidgetClass != NULL) {
    XtInitializeWidgetClass(xmFormWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 745); n++;
  XtSetArg(args[n], "width", 815); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.form = XtCreateWidget("form", xmFormWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.form);
  
  
  
  /* ---------- Creating Widget:    xrtTabManager -------------- */
  parent = self->base.form;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtTabManagerWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtTabManagerWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 3); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 3); n++;
  XtSetArg(args[n], "resizable", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 3); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 3); n++;


  /* Creating Widget */
  self->base.xrtTabManager = XtCreateWidget("xrtTabManager", xmXrtTabManagerWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtTabManager);
  
  
  
  /* ---------- Creating Widget:    xrtTabButton1 -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtTabButtonWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtTabButtonWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 58); n++;
  XtSetArg(args[n], "width", 35); n++;
  XtSetArg(args[n], "x", 4); n++;
  XtSetArg(args[n], "y", 6); n++;
  XtSetArg(args[n], "xrtGearStringRotation", 1); n++;
  XtSetArg(args[n], "xrtGearChildType", 1); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 1); n++;


  /* Creating Widget */
  self->base.xrtTabButton1 = XtCreateWidget("xrtTabButton1", xmXrtTabButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtTabButton1);
  
  
  
  /* ---------- Creating Widget:    xrt3d -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xtXrt3dWidgetClass != NULL) {
    XtInitializeWidgetClass(xtXrt3dWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "xrt3dBackgroundColor", "peachpuff"); n++;
  XtSetArg(args[n], "xrt3dDrawMesh", 1); n++;
  XtSetArg(args[n], "xrt3dDrawZones", 1); n++;
  XtSetArg(args[n], "xrt3dFooterBorder", 1); n++;
  XtSetArg(args[n], "xrt3dFooterStrings", RES_CONVERT("Xrt3dStrings", "The World")); n++;
  XtSetArg(args[n], "xrt3dGraphBorder", 1); n++;
  XtSetArg(args[n], "xrt3dLegendBorder", 1); n++;
  XtSetArg(args[n], "xrt3dSolidSurface", 0); n++;
  XtSetArg(args[n], "xrt3dSurfaceData", RES_CONVERT("Xrt3dData", "world_spline.dat")); n++;
  XtSetArg(args[n], "xrt3dXRotation", RES_CONVERT("Xrt3dDouble", "90")); n++;
  XtSetArg(args[n], "xrt3dYRotation", RES_CONVERT("Xrt3dDouble", "0")); n++;
  XtSetArg(args[n], "xrt3dZRotation", RES_CONVERT("Xrt3dDouble", "0")); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 1); n++;


  /* Creating Widget */
  self->base.xrt3d = XtCreateWidget("xrt3d", xtXrt3dWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrt3d);
  
  
  
  /* ---------- Creating Widget:    xrtTabButton2 -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtTabButtonWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtTabButtonWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 52); n++;
  XtSetArg(args[n], "width", 31); n++;
  XtSetArg(args[n], "x", 4); n++;
  XtSetArg(args[n], "y", 6); n++;
  XtSetArg(args[n], "xrtGearStringRotation", 1); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 2); n++;


  /* Creating Widget */
  self->base.xrtTabButton2 = XtCreateWidget("xrtTabButton2", xmXrtTabButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtTabButton2);
  
  
  
  /* ---------- Creating Widget:    aligner -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtAlignerWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtAlignerWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 675); n++;
  XtSetArg(args[n], "width", 801); n++;
  XtSetArg(args[n], "x", 4); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 2); n++;


  /* Creating Widget */
  self->base.aligner = XtCreateWidget("aligner", xmXrtAlignerWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.aligner);
  
  
  
  /* ---------- Creating Widget:    xrtStringField -------------- */
  parent = self->base.aligner;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtStringFieldWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtStringFieldWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.xrtStringField = XtCreateWidget("xrtStringField", xmXrtStringFieldWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtStringField);
  
  
  
  /* ---------- Creating Widget:    xrtDateField -------------- */
  parent = self->base.aligner;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtDateFieldWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtDateFieldWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.xrtDateField = XtCreateWidget("xrtDateField", xmXrtDateFieldWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtDateField);
  
  
  
  /* ---------- Creating Widget:    xrtCurrencyField -------------- */
  parent = self->base.aligner;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtCurrencyFieldWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtCurrencyFieldWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.xrtCurrencyField = XtCreateWidget("xrtCurrencyField", xmXrtCurrencyFieldWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtCurrencyField);
  
  
  
  /* ---------- Creating Widget:    xrtFloatField -------------- */
  parent = self->base.aligner;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtFloatFieldWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtFloatFieldWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.xrtFloatField = XtCreateWidget("xrtFloatField", xmXrtFloatFieldWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtFloatField);
  
  
  
  /* ---------- Creating Widget:    xrtIntField -------------- */
  parent = self->base.aligner;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtIntFieldWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtIntFieldWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  self->base.xrtIntField = XtCreateWidget("xrtIntField", xmXrtIntFieldWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtIntField);
  
  
  
  /* ---------- Creating Widget:    xrtTabButton3 -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xmXrtTabButtonWidgetClass != NULL) {
    XtInitializeWidgetClass(xmXrtTabButtonWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 52); n++;
  XtSetArg(args[n], "width", 31); n++;
  XtSetArg(args[n], "x", 4); n++;
  XtSetArg(args[n], "y", 6); n++;
  XtSetArg(args[n], "xrtGearStringRotation", 1); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 3); n++;


  /* Creating Widget */
  self->base.xrtTabButton3 = XtCreateWidget("xrtTabButton3", xmXrtTabButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.xrtTabButton3);
  
  
  
  /* ---------- Creating Widget:    table -------------- */
  parent = self->base.xrtTabManager;

  /* Converters must be registered, so call XtInitializeWidgetClass() */
  if(xtXrtTableWidgetClass != NULL) {
    XtInitializeWidgetClass(xtXrtTableWidgetClass);
  }

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 675); n++;
  XtSetArg(args[n], "width", 801); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "xrtTblAlignmentContext", 4); n++;
  XtSetArg(args[n], "xrtTblAllowCellResize", 2); n++;
  XtSetArg(args[n], "xrtTblBackgroundSeries", RES_CONVERT("xrtTblColorSeries", "(ALL ALL peachpuff)")); n++;
  XtSetArg(args[n], "xrtTblBorderTypeContext", 6); n++;
  XtSetArg(args[n], "xrtTblBottomShadowContext", RES_CONVERT("Pixel", "BlanchedAlmond")); n++;
  XtSetArg(args[n], "xrtTblColumnLabels", RES_CONVERT("xrtTblXmStringList", "Hello, VDX - User")); n++;
  XtSetArg(args[n], "xrtTblDataTypeContext", 10); n++;
  XtSetArg(args[n], "xrtTblDoubleBuffer", 2); n++;
  XtSetArg(args[n], "xrtTblEditableContext", 1); n++;
  XtSetArg(args[n], "xrtTblForegroundSeries", RES_CONVERT("xrtTblColorSeries", "(ALL LABEL red)(LABEL ALL green)")); n++;
  XtSetArg(args[n], "xrtTblFrameBorderType", 1); n++;
  XtSetArg(args[n], "xrtTblFrozenColumns", 1); n++;
  XtSetArg(args[n], "xrtTblFrozenRows", 1); n++;
  XtSetArg(args[n], "xrtTblNumRows", 40); n++;
  XtSetArg(args[n], "xrtTblRowLabels", RES_CONVERT("xrtTblXmStringList", "Row 1, Row 2, Row 3")); n++;
  XtSetArg(args[n], "xrtTblSelectionPolicy", 3); n++;
  XtSetArg(args[n], "xrtTblSetTextCursorPos", 1); n++;
  XtSetArg(args[n], "xrtTblTextInitSelect", 1); n++;
  XtSetArg(args[n], "xrtTblValidatePolicy", 3); n++;
  XtSetArg(args[n], "xrtGearPageNumber", 3); n++;


  /* Creating Widget */
  self->base.table = XtCreateWidget("table", xtXrtTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(self->base.table);
  
  

}



/* ----------------------------------------- */
/* Add Callbacks                             */
/* ----------------------------------------- */

static void TabManager_addCallbacks(self)
TabManager *self;
{

}



/* ----------------------------------------- */
/* Set resources of type widget              */
/* ----------------------------------------- */

void TabManager_addRelations(self)
TabManager *self;
{

}



/* ----------------------------------------- */
/* Now follows the callbacks                 */
/* ----------------------------------------- */





