/*
 * All files in this project are distributed under the GNU General
 * Public License. This is Free Software.
 */
// ---------------------------------------
// AttachmentEditor_base.C
//
// This file was automaticly generated by
// View Designer/X v1.2
//
// User: lazy@apfel
// Date: Tue Jun  2 17:02:49 1998
//
// Don't edit this file, please !
// ---------------------------------------


// Include class definition

#include "AttachmentEditor_base.h"

// Include X11 header
#include <X11/xpm.h>

// Include OSF/Motif header files
#include <Xm/Frame.h>
#include <X11/Shell.h>
#include <Xm/MainW.h>
#include <Xm/RowColumn.h>
#include <Xm/CascadeB.h>
#include <Xm/PushB.h>
#include <Xm/ToggleB.h>
#include <Xm/Separator.h>
#include <lwid/CellTable.h>
#include <Xm/Label.h>
#include <Xm/Frame.h>
#include <lwid/ButtonBox.h>
#include <Xm/Text.h>



#include "AttachNone.xpm"
#include "BottomAttachForm.xpm"
#include "BottomAttachWidget.xpm"
#include "BottomAttachPosition.xpm"
#include "BottomAttachSelf.xpm"
#include "LeftAttachForm.xpm"
#include "LeftAttachWidget.xpm"
#include "LeftAttachPosition.xpm"
#include "LeftAttachSelf.xpm"
#include "RightAttachForm.xpm"
#include "RightAttachWidget.xpm"
#include "RightAttachPosition.xpm"
#include "RightAttachSelf.xpm"
#include "TopAttachForm.xpm"
#include "TopAttachWidget.xpm"
#include "TopAttachPosition.xpm"
#include "TopAttachSelf.xpm"





#define RES_CONVERT(t,s) cvtResourceFromString(parent, t, s)


// -----------------------------------
// AttachmentEditor_base(parent,args,n)
// -----------------------------------

AttachmentEditor_base::AttachmentEditor_base(Widget p, ArgList f_args, Cardinal f_n, char *fbRes)
{
  Widget parent;
  Arg args[100];
  Cardinal n = 0;

  if(p) 
    parent = p;
  else
    parent = getTopLevel();

  if(fbRes) {
    XrmDatabase database, fallback;
    
    fallback = XrmGetStringDatabase(fbRes);
    database = XrmGetDatabase(XtDisplay(parent));
    
    XrmCombineDatabase(fallback, &database, FALSE);
  }

  if(!XtIsShell(parent)) {
    AttachmentEditor = XtCreateWidget("AttachmentEditor",xmFrameWidgetClass,
				       parent,f_args,f_n);
  } else { 
   
    popupShell = AttachmentEditor = XtCreatePopupShell("AttachmentEditor", topLevelShellWidgetClass,
			parent, args, n);
  }
  createWidgets();
  addCallbacks();
  addRelations();
}


// ----------------------------
// createWidgets()
// ----------------------------

void AttachmentEditor_base::createWidgets()
{
  Widget parent;           // the parent for every widget
  Arg args[100];           // resource list
  Cardinal n;              // number of resources

  // Now we create every Widget

  
  /* ---------- Creating Widget:    mainWindow_1 -------------- */
  parent = AttachmentEditor;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  mainWindow_1 = XtCreateWidget("mainWindow_1", xmMainWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(mainWindow_1);
  
  addQuickHelp(mainWindow_1);
  addHelpId(mainWindow_1,45);

  
  /* ---------- Creating Widget:    menuBar -------------- */
  parent = mainWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  menuBar = XmCreateMenuBar( parent, "menuBar", args, n);
  
  /* Manage the Widget */
  XtManageChild(menuBar);
  
  addQuickHelp(menuBar);
  addHelpId(menuBar,-1);

  
  /* ---------- Creating Widget:    fileButton -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  fileButton = XtCreateWidget("fileButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(fileButton);
  
  addQuickHelp(fileButton);
  addHelpId(fileButton,-1);

  
  /* ---------- Creating Widget:    fileMenu -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  fileMenu = XmCreatePulldownMenu( parent, "fileMenu", args, n);
  
  addQuickHelp(fileMenu);
  addHelpId(fileMenu,-1);

  
  /* ---------- Creating Widget:    loadButton -------------- */
  parent = fileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  loadButton = XtCreateWidget("loadButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(loadButton);
  
  addQuickHelp(loadButton);
  addHelpId(loadButton,-1);

  
  /* ---------- Creating Widget:    applyButton -------------- */
  parent = fileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "mnemonic", RES_CONVERT("KeySym", "A")); n++;


  /* Creating Widget */
  applyButton = XtCreateWidget("applyButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(applyButton);
  
  addQuickHelp(applyButton);
  addHelpId(applyButton,-1);

  
  /* ---------- Creating Widget:    closeButton -------------- */
  parent = fileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "accelerator", "Ctrl<Key>c"); n++;
  XtSetArg(args[n], "acceleratorText", RES_CONVERT("XmString", "^C")); n++;


  /* Creating Widget */
  closeButton = XtCreateWidget("closeButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(closeButton);
  
  addQuickHelp(closeButton);
  addHelpId(closeButton,-1);

  
  /* ---------- Creating Widget:    viewButton -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "mnemonic", RES_CONVERT("KeySym", "V")); n++;


  /* Creating Widget */
  viewButton = XtCreateWidget("viewButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(viewButton);
  
  addQuickHelp(viewButton);
  addHelpId(viewButton,-1);

  
  /* ---------- Creating Widget:    viewMenu -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  viewMenu = XmCreatePulldownMenu( parent, "viewMenu", args, n);
  
  addQuickHelp(viewMenu);
  addHelpId(viewMenu,-1);

  
  /* ---------- Creating Widget:    smallIconButton -------------- */
  parent = viewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  smallIconButton = XtCreateWidget("smallIconButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(smallIconButton);
  
  addQuickHelp(smallIconButton);
  addHelpId(smallIconButton,-1);

  
  /* ---------- Creating Widget:    withIconButton -------------- */
  parent = viewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  withIconButton = XtCreateWidget("withIconButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(withIconButton);
  
  addQuickHelp(withIconButton);
  addHelpId(withIconButton,-1);

  
  /* ---------- Creating Widget:    withClassNameButton -------------- */
  parent = viewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  withClassNameButton = XtCreateWidget("withClassNameButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(withClassNameButton);
  
  addQuickHelp(withClassNameButton);
  addHelpId(withClassNameButton,-1);

  
  /* ---------- Creating Widget:    widgetButton -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  widgetButton = XtCreateWidget("widgetButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(widgetButton);
  
  addQuickHelp(widgetButton);
  addHelpId(widgetButton,-1);

  
  /* ---------- Creating Widget:    widgetMenu -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  widgetMenu = XmCreatePulldownMenu( parent, "widgetMenu", args, n);
  
  addQuickHelp(widgetMenu);
  addHelpId(widgetMenu,-1);

  
  /* ---------- Creating Widget:    editButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  editButton = XtCreateWidget("editButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(editButton);
  
  addQuickHelp(editButton);
  addHelpId(editButton,-1);

  
  /* ---------- Creating Widget:    browseButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  browseButton = XtCreateWidget("browseButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(browseButton);
  
  addQuickHelp(browseButton);
  addHelpId(browseButton,-1);

  
  /* ---------- Creating Widget:    menuEditButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  menuEditButton = XtCreateWidget("menuEditButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditButton);
  
  addQuickHelp(menuEditButton);
  addHelpId(menuEditButton,-1);

  
  /* ---------- Creating Widget:    cutButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  cutButton = XtCreateWidget("cutButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(cutButton);
  
  addQuickHelp(cutButton);
  addHelpId(cutButton,-1);

  
  /* ---------- Creating Widget:    copyButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  copyButton = XtCreateWidget("copyButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(copyButton);
  
  addQuickHelp(copyButton);
  addHelpId(copyButton,-1);

  
  /* ---------- Creating Widget:    pasteButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  pasteButton = XtCreateWidget("pasteButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(pasteButton);
  
  addQuickHelp(pasteButton);
  addHelpId(pasteButton,-1);

  
  /* ---------- Creating Widget:    separator -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  separator = XtCreateWidget("separator", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator);
  
  addQuickHelp(separator);
  addHelpId(separator,-1);

  
  /* ---------- Creating Widget:    deleteButton -------------- */
  parent = widgetMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  deleteButton = XtCreateWidget("deleteButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(deleteButton);
  
  addQuickHelp(deleteButton);
  addHelpId(deleteButton,-1);

  
  /* ---------- Creating Widget:    helpButton -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  helpButton = XtCreateWidget("helpButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(helpButton);
  
  addQuickHelp(helpButton);
  addHelpId(helpButton,-1);

  
  /* ---------- Creating Widget:    helpMenu -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  helpMenu = XmCreatePulldownMenu( parent, "helpMenu", args, n);
  
  addQuickHelp(helpMenu);
  addHelpId(helpMenu,-1);

  
  /* ---------- Creating Widget:    onHelp -------------- */
  parent = helpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  onHelp = XtCreateWidget("onHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(onHelp);
  
  addQuickHelp(onHelp);
  addHelpId(onHelp,-1);

  
  /* ---------- Creating Widget:    onContext -------------- */
  parent = helpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "On Context")); n++;


  /* Creating Widget */
  onContext = XtCreateWidget("onContext", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(onContext);
  
  addQuickHelp(onContext);
  addHelpId(onContext,-1);

  
  /* ---------- Creating Widget:    onWidResEditor -------------- */
  parent = helpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "On Window")); n++;
  XtSetArg(args[n], "mnemonic", RES_CONVERT("KeySym", "W")); n++;


  /* Creating Widget */
  onWidResEditor = XtCreateWidget("onWidResEditor", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(onWidResEditor);
  
  addQuickHelp(onWidResEditor);
  addHelpId(onWidResEditor,-1);

  
  /* ---------- Creating Widget:    optionsButton -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  optionsButton = XtCreateWidget("optionsButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(optionsButton);
  
  addQuickHelp(optionsButton);
  addHelpId(optionsButton,-1);

  
  /* ---------- Creating Widget:    optionsMenu -------------- */
  parent = menuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  optionsMenu = XmCreatePulldownMenu( parent, "optionsMenu", args, n);
  
  addQuickHelp(optionsMenu);
  addHelpId(optionsMenu,-1);

  
  /* ---------- Creating Widget:    automaticLoadButton -------------- */
  parent = optionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  automaticLoadButton = XtCreateWidget("automaticLoadButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(automaticLoadButton);
  
  addQuickHelp(automaticLoadButton);
  addHelpId(automaticLoadButton,-1);

  
  /* ---------- Creating Widget:    topCellTable -------------- */
  parent = mainWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "resizeColumn", 0); n++;
  XtSetArg(args[n], "resizeRow", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 0); n++;


  /* Creating Widget */
  topCellTable = XtCreateWidget("topCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topCellTable);
  
  addQuickHelp(topCellTable);
  addHelpId(topCellTable,-1);

  
  /* ---------- Creating Widget:    quickHelpLabel -------------- */
  parent = topCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  quickHelpLabel = XtCreateWidget("quickHelpLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(quickHelpLabel);
  
  addQuickHelp(quickHelpLabel);
  addHelpId(quickHelpLabel,-1);

  
  /* ---------- Creating Widget:    mainCellTable -------------- */
  parent = topCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "resizeColumn", 0); n++;
  XtSetArg(args[n], "resizeRow", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  mainCellTable = XtCreateWidget("mainCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(mainCellTable);
  
  addQuickHelp(mainCellTable);
  addHelpId(mainCellTable,-1);

  
  /* ---------- Creating Widget:    childrenTable -------------- */
  parent = mainCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "resizeColumn", 0); n++;
  XtSetArg(args[n], "resizeRow", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  childrenTable = XtCreateWidget("childrenTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(childrenTable);
  
  addQuickHelp(childrenTable);
  addHelpId(childrenTable,-1);

  
  /* ---------- Creating Widget:    childrenFrame -------------- */
  parent = childrenTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 5); n++;
  XtSetArg(args[n], "marginWidth", 5); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  childrenFrame = XtCreateWidget("childrenFrame", xmFrameWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(childrenFrame);
  
  addQuickHelp(childrenFrame);
  addHelpId(childrenFrame,-1);

  
  /* ---------- Creating Widget:    childrenLabel -------------- */
  parent = childrenFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "childType", 2); n++;


  /* Creating Widget */
  childrenLabel = XtCreateWidget("childrenLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(childrenLabel);
  
  addQuickHelp(childrenLabel);
  addHelpId(childrenLabel,-1);

  
  /* ---------- Creating Widget:    buttonBox -------------- */
  parent = childrenTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "spacing", 5); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  buttonBox = XtCreateWidget("buttonBox", xlButtonBoxWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(buttonBox);
  
  addQuickHelp(buttonBox);
  addHelpId(buttonBox,-1);

  
  /* ---------- Creating Widget:    pushButton_1 -------------- */
  parent = buttonBox;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Apply")); n++;


  /* Creating Widget */
  pushButton_1 = XtCreateWidget("pushButton_1", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(pushButton_1);
  
  addQuickHelp(pushButton_1);
  addHelpId(pushButton_1,-1);

  
  /* ---------- Creating Widget:    pushButton_2 -------------- */
  parent = buttonBox;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  pushButton_2 = XtCreateWidget("pushButton_2", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(pushButton_2);
  
  addQuickHelp(pushButton_2);
  addHelpId(pushButton_2,-1);

  
  /* ---------- Creating Widget:    pushButton_3 -------------- */
  parent = buttonBox;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  pushButton_3 = XtCreateWidget("pushButton_3", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(pushButton_3);
  
  addQuickHelp(pushButton_3);
  addHelpId(pushButton_3,-1);

  
  /* ---------- Creating Widget:    attachmentCellTable -------------- */
  parent = mainCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  attachmentCellTable = XtCreateWidget("attachmentCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(attachmentCellTable);
  
  addQuickHelp(attachmentCellTable);
  addHelpId(attachmentCellTable,-1);

  
  /* ---------- Creating Widget:    xlCellTable_1 -------------- */
  parent = attachmentCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "resizeColumn", 1); n++;
  XtSetArg(args[n], "resizeRow", 1); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  xlCellTable_1 = XtCreateWidget("xlCellTable_1", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_1);
  
  addQuickHelp(xlCellTable_1);
  addHelpId(xlCellTable_1,-1);

  
  /* ---------- Creating Widget:    resizableToggle -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 1); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  resizableToggle = XtCreateWidget("resizableToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(resizableToggle);
  
  addQuickHelp(resizableToggle);
  addHelpId(resizableToggle,-1);

  
  /* ---------- Creating Widget:    separator_1 -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 20); n++;
  XtSetArg(args[n], "separatorType", 6); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  separator_1 = XtCreateWidget("separator_1", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_1);
  
  addQuickHelp(separator_1);
  addHelpId(separator_1,-1);

  
  /* ---------- Creating Widget:    separator_1_1 -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 20); n++;
  XtSetArg(args[n], "separatorType", 6); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  separator_1_1 = XtCreateWidget("separator_1_1", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_1_1);
  
  addQuickHelp(separator_1_1);
  addHelpId(separator_1_1,-1);

  
  /* ---------- Creating Widget:    separator_1_2 -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 20); n++;
  XtSetArg(args[n], "orientation", 1); n++;
  XtSetArg(args[n], "separatorType", 6); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  separator_1_2 = XtCreateWidget("separator_1_2", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_1_2);
  
  addQuickHelp(separator_1_2);
  addHelpId(separator_1_2,-1);

  
  /* ---------- Creating Widget:    separator_1_1_1 -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 20); n++;
  XtSetArg(args[n], "orientation", 1); n++;
  XtSetArg(args[n], "separatorType", 6); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 2); n++;


  /* Creating Widget */
  separator_1_1_1 = XtCreateWidget("separator_1_1_1", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_1_1_1);
  
  addQuickHelp(separator_1_1_1);
  addHelpId(separator_1_1_1,-1);

  
  /* ---------- Creating Widget:    bottomCellTable -------------- */
  parent = attachmentCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 2); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 2); n++;


  /* Creating Widget */
  bottomCellTable = XtCreateWidget("bottomCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomCellTable);
  
  addQuickHelp(bottomCellTable);
  addHelpId(bottomCellTable,-1);

  
  /* ---------- Creating Widget:    bottomAttachment -------------- */
  parent = bottomCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 110); n++;
  XtSetArg(args[n], "x", 62); n++;
  XtSetArg(args[n], "y", 5); n++;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  bottomAttachment = XmCreateOptionMenu( parent, "bottomAttachment", args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachment);
  
  addQuickHelp(bottomAttachment);
  addHelpId(bottomAttachment,-1);

  
  /* ---------- Creating Widget:    bottomAttachmentMenu -------------- */
  parent = bottomAttachment;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  bottomAttachmentMenu = XmCreatePulldownMenu( parent, "bottomAttachmentMenu", args, n);
  
  addQuickHelp(bottomAttachmentMenu);
  addHelpId(bottomAttachmentMenu,-1);

  
  /* ---------- Creating Widget:    bottomAttachNone -------------- */
  parent = bottomAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(AttachNone_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  bottomAttachNone = XtCreateWidget("bottomAttachNone", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachNone);
  
  addQuickHelp(bottomAttachNone);
  addHelpId(bottomAttachNone,-1);

  
  /* ---------- Creating Widget:    bottomAttachForm -------------- */
  parent = bottomAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(BottomAttachForm_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  bottomAttachForm = XtCreateWidget("bottomAttachForm", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachForm);
  
  addQuickHelp(bottomAttachForm);
  addHelpId(bottomAttachForm,-1);

  
  /* ---------- Creating Widget:    bottomAttachWidget -------------- */
  parent = bottomAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(BottomAttachWidget_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  bottomAttachWidget = XtCreateWidget("bottomAttachWidget", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachWidget);
  
  addQuickHelp(bottomAttachWidget);
  addHelpId(bottomAttachWidget,-1);

  
  /* ---------- Creating Widget:    bottomAttachPosition -------------- */
  parent = bottomAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(BottomAttachPosition_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  bottomAttachPosition = XtCreateWidget("bottomAttachPosition", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachPosition);
  
  addQuickHelp(bottomAttachPosition);
  addHelpId(bottomAttachPosition,-1);

  
  /* ---------- Creating Widget:    bottomAttachSelf -------------- */
  parent = bottomAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(BottomAttachSelf_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  bottomAttachSelf = XtCreateWidget("bottomAttachSelf", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomAttachSelf);
  
  addQuickHelp(bottomAttachSelf);
  addHelpId(bottomAttachSelf,-1);

  
  /* ---------- Creating Widget:    bottomWidget -------------- */
  parent = bottomCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  bottomWidget = XmCreateOptionMenu( parent, "bottomWidget", args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomWidget);
  
  addQuickHelp(bottomWidget);
  addHelpId(bottomWidget,-1);

  
  /* ---------- Creating Widget:    xlCellTable_5 -------------- */
  parent = bottomCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  xlCellTable_5 = XtCreateWidget("xlCellTable_5", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_5);
  
  addQuickHelp(xlCellTable_5);
  addHelpId(xlCellTable_5,-1);

  
  /* ---------- Creating Widget:    bottomOffsetLabel -------------- */
  parent = xlCellTable_5;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  bottomOffsetLabel = XtCreateWidget("bottomOffsetLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomOffsetLabel);
  
  addQuickHelp(bottomOffsetLabel);
  addHelpId(bottomOffsetLabel,-1);

  
  /* ---------- Creating Widget:    bottomPositionLabel -------------- */
  parent = xlCellTable_5;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  bottomPositionLabel = XtCreateWidget("bottomPositionLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomPositionLabel);
  
  addQuickHelp(bottomPositionLabel);
  addHelpId(bottomPositionLabel,-1);

  
  /* ---------- Creating Widget:    bottomOffset -------------- */
  parent = xlCellTable_5;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  bottomOffset = XtCreateWidget("bottomOffset", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomOffset);
  
  addQuickHelp(bottomOffset);
  addHelpId(bottomOffset,-1);

  
  /* ---------- Creating Widget:    bottomPosition -------------- */
  parent = xlCellTable_5;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 3); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  bottomPosition = XtCreateWidget("bottomPosition", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(bottomPosition);
  
  addQuickHelp(bottomPosition);
  addHelpId(bottomPosition,-1);

  
  /* ---------- Creating Widget:    leftCellTable -------------- */
  parent = attachmentCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 2); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 2); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  leftCellTable = XtCreateWidget("leftCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftCellTable);
  
  addQuickHelp(leftCellTable);
  addHelpId(leftCellTable,-1);

  
  /* ---------- Creating Widget:    leftAttachment -------------- */
  parent = leftCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 93); n++;
  XtSetArg(args[n], "x", 62); n++;
  XtSetArg(args[n], "y", 5); n++;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  leftAttachment = XmCreateOptionMenu( parent, "leftAttachment", args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachment);
  
  addQuickHelp(leftAttachment);
  addHelpId(leftAttachment,-1);

  
  /* ---------- Creating Widget:    leftAttachmentMenu -------------- */
  parent = leftAttachment;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  leftAttachmentMenu = XmCreatePulldownMenu( parent, "leftAttachmentMenu", args, n);
  
  addQuickHelp(leftAttachmentMenu);
  addHelpId(leftAttachmentMenu,-1);

  
  /* ---------- Creating Widget:    leftAttachNone -------------- */
  parent = leftAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(AttachNone_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  leftAttachNone = XtCreateWidget("leftAttachNone", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachNone);
  
  addQuickHelp(leftAttachNone);
  addHelpId(leftAttachNone,-1);

  
  /* ---------- Creating Widget:    leftAttachForm -------------- */
  parent = leftAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(LeftAttachForm_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  leftAttachForm = XtCreateWidget("leftAttachForm", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachForm);
  
  addQuickHelp(leftAttachForm);
  addHelpId(leftAttachForm,-1);

  
  /* ---------- Creating Widget:    leftAttachWidget -------------- */
  parent = leftAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(LeftAttachWidget_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  leftAttachWidget = XtCreateWidget("leftAttachWidget", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachWidget);
  
  addQuickHelp(leftAttachWidget);
  addHelpId(leftAttachWidget,-1);

  
  /* ---------- Creating Widget:    leftAttachPosition -------------- */
  parent = leftAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(LeftAttachPosition_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  leftAttachPosition = XtCreateWidget("leftAttachPosition", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachPosition);
  
  addQuickHelp(leftAttachPosition);
  addHelpId(leftAttachPosition,-1);

  
  /* ---------- Creating Widget:    leftAttachSelf -------------- */
  parent = leftAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(LeftAttachSelf_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  leftAttachSelf = XtCreateWidget("leftAttachSelf", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftAttachSelf);
  
  addQuickHelp(leftAttachSelf);
  addHelpId(leftAttachSelf,-1);

  
  /* ---------- Creating Widget:    leftWidget -------------- */
  parent = leftCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  leftWidget = XmCreateOptionMenu( parent, "leftWidget", args, n);
  
  /* Manage the Widget */
  XtManageChild(leftWidget);
  
  addQuickHelp(leftWidget);
  addHelpId(leftWidget,-1);

  
  /* ---------- Creating Widget:    xlCellTable_3 -------------- */
  parent = leftCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  xlCellTable_3 = XtCreateWidget("xlCellTable_3", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_3);
  
  addQuickHelp(xlCellTable_3);
  addHelpId(xlCellTable_3,-1);

  
  /* ---------- Creating Widget:    leftOffsetLabel -------------- */
  parent = xlCellTable_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  leftOffsetLabel = XtCreateWidget("leftOffsetLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftOffsetLabel);
  
  addQuickHelp(leftOffsetLabel);
  addHelpId(leftOffsetLabel,-1);

  
  /* ---------- Creating Widget:    leftPositionLabel -------------- */
  parent = xlCellTable_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  leftPositionLabel = XtCreateWidget("leftPositionLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftPositionLabel);
  
  addQuickHelp(leftPositionLabel);
  addHelpId(leftPositionLabel,-1);

  
  /* ---------- Creating Widget:    leftOffset -------------- */
  parent = xlCellTable_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  leftOffset = XtCreateWidget("leftOffset", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftOffset);
  
  addQuickHelp(leftOffset);
  addHelpId(leftOffset,-1);

  
  /* ---------- Creating Widget:    leftPosition -------------- */
  parent = xlCellTable_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 3); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  leftPosition = XtCreateWidget("leftPosition", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(leftPosition);
  
  addQuickHelp(leftPosition);
  addHelpId(leftPosition,-1);

  
  /* ---------- Creating Widget:    rightCellTable -------------- */
  parent = attachmentCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 2); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  rightCellTable = XtCreateWidget("rightCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightCellTable);
  
  addQuickHelp(rightCellTable);
  addHelpId(rightCellTable,-1);

  
  /* ---------- Creating Widget:    rightAttachment -------------- */
  parent = rightCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  rightAttachment = XmCreateOptionMenu( parent, "rightAttachment", args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachment);
  
  addQuickHelp(rightAttachment);
  addHelpId(rightAttachment,-1);

  
  /* ---------- Creating Widget:    rightAttachmentMenu -------------- */
  parent = rightAttachment;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  rightAttachmentMenu = XmCreatePulldownMenu( parent, "rightAttachmentMenu", args, n);
  
  addQuickHelp(rightAttachmentMenu);
  addHelpId(rightAttachmentMenu,-1);

  
  /* ---------- Creating Widget:    rightAttachNone -------------- */
  parent = rightAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(AttachNone_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  rightAttachNone = XtCreateWidget("rightAttachNone", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachNone);
  
  addQuickHelp(rightAttachNone);
  addHelpId(rightAttachNone,-1);

  
  /* ---------- Creating Widget:    rightAttachForm -------------- */
  parent = rightAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(RightAttachForm_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  rightAttachForm = XtCreateWidget("rightAttachForm", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachForm);
  
  addQuickHelp(rightAttachForm);
  addHelpId(rightAttachForm,-1);

  
  /* ---------- Creating Widget:    rightAttachWidget -------------- */
  parent = rightAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(RightAttachWidget_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  rightAttachWidget = XtCreateWidget("rightAttachWidget", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachWidget);
  
  addQuickHelp(rightAttachWidget);
  addHelpId(rightAttachWidget,-1);

  
  /* ---------- Creating Widget:    rightAttachPosition -------------- */
  parent = rightAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(RightAttachPosition_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  rightAttachPosition = XtCreateWidget("rightAttachPosition", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachPosition);
  
  addQuickHelp(rightAttachPosition);
  addHelpId(rightAttachPosition,-1);

  
  /* ---------- Creating Widget:    rightAttachSelf -------------- */
  parent = rightAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(RightAttachSelf_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  rightAttachSelf = XtCreateWidget("rightAttachSelf", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightAttachSelf);
  
  addQuickHelp(rightAttachSelf);
  addHelpId(rightAttachSelf,-1);

  
  /* ---------- Creating Widget:    rightWidget -------------- */
  parent = rightCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  rightWidget = XmCreateOptionMenu( parent, "rightWidget", args, n);
  
  /* Manage the Widget */
  XtManageChild(rightWidget);
  
  addQuickHelp(rightWidget);
  addHelpId(rightWidget,-1);

  
  /* ---------- Creating Widget:    xlCellTable_4 -------------- */
  parent = rightCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  xlCellTable_4 = XtCreateWidget("xlCellTable_4", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_4);
  
  addQuickHelp(xlCellTable_4);
  addHelpId(xlCellTable_4,-1);

  
  /* ---------- Creating Widget:    rightOffsetLabel -------------- */
  parent = xlCellTable_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  rightOffsetLabel = XtCreateWidget("rightOffsetLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightOffsetLabel);
  
  addQuickHelp(rightOffsetLabel);
  addHelpId(rightOffsetLabel,-1);

  
  /* ---------- Creating Widget:    rightPositionLabel -------------- */
  parent = xlCellTable_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  rightPositionLabel = XtCreateWidget("rightPositionLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightPositionLabel);
  
  addQuickHelp(rightPositionLabel);
  addHelpId(rightPositionLabel,-1);

  
  /* ---------- Creating Widget:    rightOffset -------------- */
  parent = xlCellTable_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  rightOffset = XtCreateWidget("rightOffset", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightOffset);
  
  addQuickHelp(rightOffset);
  addHelpId(rightOffset,-1);

  
  /* ---------- Creating Widget:    rightPosition -------------- */
  parent = xlCellTable_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 3); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  rightPosition = XtCreateWidget("rightPosition", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rightPosition);
  
  addQuickHelp(rightPosition);
  addHelpId(rightPosition,-1);

  
  /* ---------- Creating Widget:    xlCellTable -------------- */
  parent = attachmentCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 2); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "verticalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  xlCellTable = XtCreateWidget("xlCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable);
  
  addQuickHelp(xlCellTable);
  addHelpId(xlCellTable,-1);

  
  /* ---------- Creating Widget:    topAttachment -------------- */
  parent = xlCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  topAttachment = XmCreateOptionMenu( parent, "topAttachment", args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachment);
  
  addQuickHelp(topAttachment);
  addHelpId(topAttachment,-1);

  
  /* ---------- Creating Widget:    topAttachmentMenu -------------- */
  parent = topAttachment;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  topAttachmentMenu = XmCreatePulldownMenu( parent, "topAttachmentMenu", args, n);
  
  addQuickHelp(topAttachmentMenu);
  addHelpId(topAttachmentMenu,-1);

  
  /* ---------- Creating Widget:    topAttachNone -------------- */
  parent = topAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(AttachNone_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  topAttachNone = XtCreateWidget("topAttachNone", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachNone);
  
  addQuickHelp(topAttachNone);
  addHelpId(topAttachNone,-1);

  
  /* ---------- Creating Widget:    topAttachForm -------------- */
  parent = topAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(TopAttachForm_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  topAttachForm = XtCreateWidget("topAttachForm", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachForm);
  
  addQuickHelp(topAttachForm);
  addHelpId(topAttachForm,-1);

  
  /* ---------- Creating Widget:    topAttachWidget -------------- */
  parent = topAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(TopAttachWidget_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  topAttachWidget = XtCreateWidget("topAttachWidget", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachWidget);
  
  addQuickHelp(topAttachWidget);
  addHelpId(topAttachWidget,-1);

  
  /* ---------- Creating Widget:    topAttachPosition -------------- */
  parent = topAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(TopAttachPosition_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  topAttachPosition = XtCreateWidget("topAttachPosition", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachPosition);
  
  addQuickHelp(topAttachPosition);
  addHelpId(topAttachPosition,-1);

  
  /* ---------- Creating Widget:    topAttachSelf -------------- */
  parent = topAttachmentMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromData(TopAttachSelf_xpm)); n++;
  XtSetArg(args[n], "labelType", 1); n++;


  /* Creating Widget */
  topAttachSelf = XtCreateWidget("topAttachSelf", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topAttachSelf);
  
  addQuickHelp(topAttachSelf);
  addHelpId(topAttachSelf,-1);

  
  /* ---------- Creating Widget:    topWidget -------------- */
  parent = xlCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  topWidget = XmCreateOptionMenu( parent, "topWidget", args, n);
  
  /* Manage the Widget */
  XtManageChild(topWidget);
  
  addQuickHelp(topWidget);
  addHelpId(topWidget,-1);

  
  /* ---------- Creating Widget:    xlCellTable_2 -------------- */
  parent = xlCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "horizontalSpacing", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  xlCellTable_2 = XtCreateWidget("xlCellTable_2", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_2);
  
  addQuickHelp(xlCellTable_2);
  addHelpId(xlCellTable_2,-1);

  
  /* ---------- Creating Widget:    topPositionLabel -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 2); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  topPositionLabel = XtCreateWidget("topPositionLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topPositionLabel);
  
  addQuickHelp(topPositionLabel);
  addHelpId(topPositionLabel,-1);

  
  /* ---------- Creating Widget:    topOffsetLabel -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  topOffsetLabel = XtCreateWidget("topOffsetLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topOffsetLabel);
  
  addQuickHelp(topOffsetLabel);
  addHelpId(topOffsetLabel,-1);

  
  /* ---------- Creating Widget:    topOffset -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  topOffset = XtCreateWidget("topOffset", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topOffset);
  
  addQuickHelp(topOffset);
  addHelpId(topOffset,-1);

  
  /* ---------- Creating Widget:    topPosition -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "columns", 3); n++;
  XtSetArg(args[n], "marginHeight", 2); n++;
  XtSetArg(args[n], "marginWidth", 2); n++;
  XtSetArg(args[n], "cellColumn", 3); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  topPosition = XtCreateWidget("topPosition", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(topPosition);
  
  addQuickHelp(topPosition);
  addHelpId(topPosition,-1);


}



// ----------------------------
// addCallbacks()
// ----------------------------

void AttachmentEditor_base::addCallbacks()
{

  XtAddCallback( loadButton, "activateCallback",
                 do_load_CB, this);

  XtAddCallback( applyButton, "activateCallback",
                 do_apply_CB, this);

  XtAddCallback( closeButton, "activateCallback",
                 do_close_CB, this);

  XtAddCallback( smallIconButton, "valueChangedCallback",
                 do_smallIcon_CB, this);

  XtAddCallback( withIconButton, "valueChangedCallback",
                 do_withIcon_CB, this);

  XtAddCallback( withClassNameButton, "valueChangedCallback",
                 do_withClassName_CB, this);

  XtAddCallback( widgetMenu, "mapCallback",
                 do_widgetMenuMap_CB, this);

  XtAddCallback( onHelp, "activateCallback",
                 do_onHelp_CB, this);

  XtAddCallback( onContext, "activateCallback",
                 do_onContext_CB, this);

  XtAddCallback( onWidResEditor, "activateCallback",
                 do_help_CB, this);

  XtAddCallback( automaticLoadButton, "valueChangedCallback",
                 do_automaticLoad_CB, this);

  XtAddCallback( pushButton_1, "activateCallback",
                 do_apply_CB, this);

  XtAddCallback( pushButton_2, "activateCallback",
                 do_close_CB, this);

  XtAddCallback( pushButton_3, "activateCallback",
                 do_help_CB, this);

  XtAddCallback( bottomAttachmentMenu, "entryCallback",
                 bottomAttachmentChanged_CB, this);

  XtAddCallback( bottomOffset, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( bottomPosition, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( leftAttachmentMenu, "entryCallback",
                 leftAttachmentChanged_CB, this);

  XtAddCallback( leftOffset, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( leftPosition, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( rightAttachmentMenu, "entryCallback",
                 rightAttachmentChanged_CB, this);

  XtAddCallback( rightOffset, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( rightPosition, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( topAttachmentMenu, "entryCallback",
                 topAttachmentChanged_CB, this);

  XtAddCallback( topOffset, "valueChangedCallback",
                 valueChanged_CB, this);

  XtAddCallback( topPosition, "valueChangedCallback",
                 valueChanged_CB, this);

}



// ----------------------------
// addRelations()
// ----------------------------

void AttachmentEditor_base::addRelations()
{
  XtVaSetValues(menuBar, "menuHelpWidget", helpButton, NULL);

  XtVaSetValues(fileButton, "subMenuId", fileMenu, NULL);

  XtVaSetValues(viewButton, "subMenuId", viewMenu, NULL);

  XtVaSetValues(widgetButton, "subMenuId", widgetMenu, NULL);

  XtVaSetValues(helpButton, "subMenuId", helpMenu, NULL);

  XtVaSetValues(optionsButton, "subMenuId", optionsMenu, NULL);

  XtVaSetValues(bottomAttachment, "subMenuId", bottomAttachmentMenu, NULL);

  XtVaSetValues(leftAttachment, "subMenuId", leftAttachmentMenu, NULL);

  XtVaSetValues(rightAttachment, "subMenuId", rightAttachmentMenu, NULL);

  XtVaSetValues(topAttachment, "subMenuId", topAttachmentMenu, NULL);


}


// ----------------------------
// manageWidget()
// ----------------------------
void AttachmentEditor_base::manageWidget(Widget, XtPointer c, XtPointer)
{
  XtManageChild((Widget)c);
}


// ----------------------------
// createPixmapFromData()
// ----------------------------

Pixmap AttachmentEditor_base::createPixmapFromData(char **data)
{
  Pixmap pix, mask;
  XpmAttributes attr;

  memset(&attr, 0, sizeof(attr));
  attr.valuemask = XpmReturnPixels;

  if( !XpmCreatePixmapFromData(XtDisplay(LicView::getTopLevel()),
		               XRootWindowOfScreen(XtScreen(LicView::getTopLevel())),
			       data,
			       &pix, &mask, &attr) )
    return pix;
  return XmUNSPECIFIED_PIXMAP;
}

// ------------------------------------------
// createPixmapFromFile()
//
// Creating a pixmap by XpmRaedFileToPixmap()
// Uses XFILESEARCHPATH to find the pixmap
// default path: /usr/lib/X11/pixmaps/%N.xpm
// ------------------------------------------

Pixmap AttachmentEditor_base::createPixmapFromFile(char *name)
{
  String string;
  Pixmap pix, mask;
  XpmAttributes attr;

  string = XtResolvePathname(XtDisplay(LicView::getTopLevel()),
			     "pixmaps", name, "", NULL,
			     NULL, 0, NULL);
  if(string) {
    memset(&attr, 0, sizeof(attr));
    attr.valuemask = XpmReturnPixels;
    if( !XpmReadFileToPixmap(XtDisplay(LicView::getTopLevel()),
			     XRootWindowOfScreen(
			     XtScreen(LicView::getTopLevel())),
			     string,
			     &pix, &mask, &attr) )
      return pix;
    return XmUNSPECIFIED_PIXMAP;
    XtFree(string);
  }
  return XmUNSPECIFIED_PIXMAP;
}

// ---------------------------------------------------------
// Following class method are used as a callback.
// The method is calling a abstract virtual method, which
// must be overloaded in the derived class 'AttachmentEditor'
// ---------------------------------------------------------

void AttachmentEditor_base::do_load_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_load(wg,callData);
}

void AttachmentEditor_base::do_apply_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_apply(wg,callData);
}

void AttachmentEditor_base::do_close_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_close(wg,callData);
}

void AttachmentEditor_base::do_smallIcon_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_smallIcon(wg,callData);
}

void AttachmentEditor_base::do_withIcon_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_withIcon(wg,callData);
}

void AttachmentEditor_base::do_withClassName_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_withClassName(wg,callData);
}

void AttachmentEditor_base::do_widgetMenuMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_widgetMenuMap(wg,callData);
}

void AttachmentEditor_base::do_onHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_onHelp(wg,callData);
}

void AttachmentEditor_base::do_onContext_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_onContext(wg,callData);
}

void AttachmentEditor_base::do_help_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_help(wg,callData);
}

void AttachmentEditor_base::do_automaticLoad_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->do_automaticLoad(wg,callData);
}

void AttachmentEditor_base::bottomAttachmentChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->bottomAttachmentChanged(wg,callData);
}

void AttachmentEditor_base::valueChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->valueChanged(wg,callData);
}

void AttachmentEditor_base::leftAttachmentChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->leftAttachmentChanged(wg,callData);
}

void AttachmentEditor_base::rightAttachmentChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->rightAttachmentChanged(wg,callData);
}

void AttachmentEditor_base::topAttachmentChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((AttachmentEditor_base*)clientData)->topAttachmentChanged(wg,callData);
}


