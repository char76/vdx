/*
 * All files in this project are distributed under the GNU General
 * Public License. This is Free Software.
 */
// ---------------------------------------
// MainWindow_base.C
//
// This file was automaticly generated by
// View Designer/X v1.2
//
// User: lazy@apfel
// Date: Tue Jun  2 17:02:45 1998
//
// Don't edit this file, please !
// ---------------------------------------


// Include class definition

#include "MainWindow_base.h"

// Include X11 header
#include <X11/xpm.h>

// Include OSF/Motif header files
#include <Xm/Frame.h>
#include <X11/Shell.h>
#include <Xm/Form.h>
#include <Xm/RowColumn.h>
#include <Xm/CascadeB.h>
#include <Xm/PushB.h>
#include <Xm/Separator.h>
#include <Xm/ToggleB.h>
#include <Xm/DrawnB.h>
#include <Xm/PanedW.h>
#include <Xm/Label.h>
#include <Xm/ScrolledW.h>
#include <Xm/Text.h>
#include <Xm/FileSB.h>
#include <Xm/Scale.h>
#include <Xm/List.h>
#include <Xm/MessageB.h>
#include <lwid/CellTable.h>
#include <Xm/Frame.h>








#define RES_CONVERT(t,s) cvtResourceFromString(parent, t, s)


// -----------------------------------
// MainWindow_base(parent,args,n)
// -----------------------------------

MainWindow_base::MainWindow_base(Widget p, ArgList f_args, Cardinal f_n, char *fbRes)
{
  Widget parent;
  Arg args[100];
  Cardinal n = 0;

  if(p) 
    parent = p;
  else
    parent = getTopLevel();

  if(fbRes) {
    XrmDatabase database, fallback;
    
    fallback = XrmGetStringDatabase(fbRes);
    database = XrmGetDatabase(XtDisplay(parent));
    
    XrmCombineDatabase(fallback, &database, FALSE);
  }

  if(!XtIsShell(parent)) {
    MainWindow = XtCreateWidget("MainWindow",xmFrameWidgetClass,
				       parent,f_args,f_n);
  } else { 
     XtSetArg(args[n], "iconPixmap", createPixmapFromFile("Logo.xpm")); n++;

    popupShell = MainWindow = XtCreatePopupShell("MainWindow", topLevelShellWidgetClass,
			parent, args, n);
  }
  createWidgets();
  addCallbacks();
  addRelations();
}


// ----------------------------
// createWidgets()
// ----------------------------

void MainWindow_base::createWidgets()
{
  Widget parent;           // the parent for every widget
  Arg args[100];           // resource list
  Cardinal n;              // number of resources

  // Now we create every Widget

  
  /* ---------- Creating Widget:    XmForm_1 -------------- */
  parent = MainWindow;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 450); n++;
  XtSetArg(args[n], "width", 500); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  XmForm_1 = XtCreateWidget("XmForm_1", xmFormWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmForm_1);
  
  addQuickHelp(XmForm_1);
  addHelpId(XmForm_1,1);

  
  /* ---------- Creating Widget:    MenuBar -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 34); n++;
  XtSetArg(args[n], "width", 498); n++;
  XtSetArg(args[n], "x", 1); n++;
  XtSetArg(args[n], "y", 1); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  MenuBar = XmCreateMenuBar( parent, "MenuBar", args, n);
  
  /* Manage the Widget */
  XtManageChild(MenuBar);
  
  addQuickHelp(MenuBar);
  addHelpId(MenuBar,-1);

  
  /* ---------- Creating Widget:    File -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  File = XtCreateWidget("File", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(File);
  
  addQuickHelp(File);
  addHelpId(File,-1);

  
  /* ---------- Creating Widget:    FileMenu -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  FileMenu = XmCreatePulldownMenu( parent, "FileMenu", args, n);
  
  addQuickHelp(FileMenu);
  addHelpId(FileMenu,-1);

  
  /* ---------- Creating Widget:    ProjectNew -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectNew = XtCreateWidget("ProjectNew", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNew);
  
  addQuickHelp(ProjectNew);
  addHelpId(ProjectNew,-1);

  
  /* ---------- Creating Widget:    FileOpen -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  FileOpen = XtCreateWidget("FileOpen", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(FileOpen);
  
  addQuickHelp(FileOpen);
  addHelpId(FileOpen,-1);

  
  /* ---------- Creating Widget:    FileSave -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  FileSave = XtCreateWidget("FileSave", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(FileSave);
  
  addQuickHelp(FileSave);
  addHelpId(FileSave,-1);

  
  /* ---------- Creating Widget:    FileSaveAs -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  FileSaveAs = XtCreateWidget("FileSaveAs", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(FileSaveAs);
  
  addQuickHelp(FileSaveAs);
  addHelpId(FileSaveAs,-1);

  
  /* ---------- Creating Widget:    separator -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  separator = XtCreateWidget("separator", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator);
  
  addQuickHelp(separator);
  addHelpId(separator,-1);

  
  /* ---------- Creating Widget:    newTemplates -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "mnemonic", RES_CONVERT("KeySym", "T")); n++;


  /* Creating Widget */
  newTemplates = XtCreateWidget("newTemplates", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(newTemplates);
  
  addQuickHelp(newTemplates);
  addHelpId(newTemplates,-1);

  
  /* ---------- Creating Widget:    openTemplates -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  openTemplates = XtCreateWidget("openTemplates", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(openTemplates);
  
  addQuickHelp(openTemplates);
  addHelpId(openTemplates,-1);

  
  /* ---------- Creating Widget:    separator_2 -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  separator_2 = XtCreateWidget("separator_2", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(separator_2);
  
  addQuickHelp(separator_2);
  addHelpId(separator_2,-1);

  
  /* ---------- Creating Widget:    FileExit -------------- */
  parent = FileMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  FileExit = XtCreateWidget("FileExit", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(FileExit);
  
  addQuickHelp(FileExit);
  addHelpId(FileExit,-1);

  
  /* ---------- Creating Widget:    Project -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  Project = XtCreateWidget("Project", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(Project);
  
  addQuickHelp(Project);
  addHelpId(Project,-1);

  
  /* ---------- Creating Widget:    ProjectMenu -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectMenu = XmCreatePulldownMenu( parent, "ProjectMenu", args, n);
  
  addQuickHelp(ProjectMenu);
  addHelpId(ProjectMenu,-1);

  
  /* ---------- Creating Widget:    ProjectSettings -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectSettings = XtCreateWidget("ProjectSettings", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectSettings);
  
  addQuickHelp(ProjectSettings);
  addHelpId(ProjectSettings,-1);

  
  /* ---------- Creating Widget:    ProjectLoadInterface -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectLoadInterface = XtCreateWidget("ProjectLoadInterface", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectLoadInterface);
  
  addQuickHelp(ProjectLoadInterface);
  addHelpId(ProjectLoadInterface,-1);

  
  /* ---------- Creating Widget:    ProjectShowHelp -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectShowHelp = XtCreateWidget("ProjectShowHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  addQuickHelp(ProjectShowHelp);
  addHelpId(ProjectShowHelp,-1);

  
  /* ---------- Creating Widget:    ProjectResources -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectResources = XtCreateWidget("ProjectResources", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectResources);
  
  addQuickHelp(ProjectResources);
  addHelpId(ProjectResources,-1);

  
  /* ---------- Creating Widget:    ProjectGenerate -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectGenerate = XtCreateWidget("ProjectGenerate", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectGenerate);
  
  addQuickHelp(ProjectGenerate);
  addHelpId(ProjectGenerate,-1);

  
  /* ---------- Creating Widget:    ProjectExecute -------------- */
  parent = ProjectMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ProjectExecute = XtCreateWidget("ProjectExecute", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectExecute);
  
  addQuickHelp(ProjectExecute);
  addHelpId(ProjectExecute,-1);

  
  /* ---------- Creating Widget:    Widgets -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  Widgets = XtCreateWidget("Widgets", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(Widgets);
  
  addQuickHelp(Widgets);
  addHelpId(Widgets,-1);

  
  /* ---------- Creating Widget:    WidgetsMenu -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  WidgetsMenu = XmCreatePulldownMenu( parent, "WidgetsMenu", args, n);
  
  addQuickHelp(WidgetsMenu);
  addHelpId(WidgetsMenu,-1);

  
  /* ---------- Creating Widget:    WidgetsPalette -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  WidgetsPalette = XtCreateWidget("WidgetsPalette", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(WidgetsPalette);
  
  addQuickHelp(WidgetsPalette);
  addHelpId(WidgetsPalette,-1);

  
  /* ---------- Creating Widget:    XmSeparator -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  XmSeparator = XtCreateWidget("XmSeparator", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmSeparator);
  
  addQuickHelp(XmSeparator);
  addHelpId(XmSeparator,-1);

  
  /* ---------- Creating Widget:    ShellMenuButton -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  ShellMenuButton = XtCreateWidget("ShellMenuButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ShellMenuButton);
  
  addQuickHelp(ShellMenuButton);
  addHelpId(ShellMenuButton,-1);

  
  /* ---------- Creating Widget:    ShellMenu -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "tearOffModel", 0); n++;


  /* Creating Widget */
  ShellMenu = XmCreatePulldownMenu( parent, "ShellMenu", args, n);
  
  addQuickHelp(ShellMenu);
  addHelpId(ShellMenu,-1);

  
  /* ---------- Creating Widget:    ManagerMenuButton -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "mnemonic", RES_CONVERT("KeySym", "M")); n++;


  /* Creating Widget */
  ManagerMenuButton = XtCreateWidget("ManagerMenuButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ManagerMenuButton);
  
  addQuickHelp(ManagerMenuButton);
  addHelpId(ManagerMenuButton,-1);

  
  /* ---------- Creating Widget:    ManagerMenu -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "tearOffModel", 0); n++;


  /* Creating Widget */
  ManagerMenu = XmCreatePulldownMenu( parent, "ManagerMenu", args, n);
  
  addQuickHelp(ManagerMenu);
  addHelpId(ManagerMenu,-1);

  
  /* ---------- Creating Widget:    PrimitivesMenuButton -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  PrimitivesMenuButton = XtCreateWidget("PrimitivesMenuButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PrimitivesMenuButton);
  
  addQuickHelp(PrimitivesMenuButton);
  addHelpId(PrimitivesMenuButton,-1);

  
  /* ---------- Creating Widget:    PrimitivesMenu -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "tearOffModel", 0); n++;


  /* Creating Widget */
  PrimitivesMenu = XmCreatePulldownMenu( parent, "PrimitivesMenu", args, n);
  
  addQuickHelp(PrimitivesMenu);
  addHelpId(PrimitivesMenu,-1);

  
  /* ---------- Creating Widget:    DialogsMenuButton -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  DialogsMenuButton = XtCreateWidget("DialogsMenuButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(DialogsMenuButton);
  
  addQuickHelp(DialogsMenuButton);
  addHelpId(DialogsMenuButton,-1);

  
  /* ---------- Creating Widget:    DialogsMenu -------------- */
  parent = WidgetsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "tearOffModel", 0); n++;


  /* Creating Widget */
  DialogsMenu = XmCreatePulldownMenu( parent, "DialogsMenu", args, n);
  
  addQuickHelp(DialogsMenu);
  addHelpId(DialogsMenu,-1);

  
  /* ---------- Creating Widget:    Options -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  Options = XtCreateWidget("Options", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(Options);
  
  addQuickHelp(Options);
  addHelpId(Options,-1);

  
  /* ---------- Creating Widget:    OptionsMenu -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  OptionsMenu = XmCreatePulldownMenu( parent, "OptionsMenu", args, n);
  
  addQuickHelp(OptionsMenu);
  addHelpId(OptionsMenu,-1);

  
  /* ---------- Creating Widget:    genericOptionButton -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  genericOptionButton = XtCreateWidget("genericOptionButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericOptionButton);
  
  addQuickHelp(genericOptionButton);
  addHelpId(genericOptionButton,-1);

  
  /* ---------- Creating Widget:    OptionsGrid -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  OptionsGrid = XtCreateWidget("OptionsGrid", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(OptionsGrid);
  
  addQuickHelp(OptionsGrid);
  addHelpId(OptionsGrid,-1);

  
  /* ---------- Creating Widget:    UpdatePolicy -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  UpdatePolicy = XtCreateWidget("UpdatePolicy", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(UpdatePolicy);
  
  addQuickHelp(UpdatePolicy);
  addHelpId(UpdatePolicy,-1);

  
  /* ---------- Creating Widget:    UpdatePolicyMenu -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "radioBehavior", 1); n++;


  /* Creating Widget */
  UpdatePolicyMenu = XmCreatePulldownMenu( parent, "UpdatePolicyMenu", args, n);
  
  addQuickHelp(UpdatePolicyMenu);
  addHelpId(UpdatePolicyMenu,-1);

  
  /* ---------- Creating Widget:    minimalRefreshButton -------------- */
  parent = UpdatePolicyMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  minimalRefreshButton = XtCreateWidget("minimalRefreshButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(minimalRefreshButton);
  
  addQuickHelp(minimalRefreshButton);
  addHelpId(minimalRefreshButton,-1);

  
  /* ---------- Creating Widget:    optimalRefreshButton -------------- */
  parent = UpdatePolicyMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  optimalRefreshButton = XtCreateWidget("optimalRefreshButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(optimalRefreshButton);
  
  addQuickHelp(optimalRefreshButton);
  addHelpId(optimalRefreshButton,-1);

  
  /* ---------- Creating Widget:    defaultShellButton -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  defaultShellButton = XtCreateWidget("defaultShellButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultShellButton);
  
  addQuickHelp(defaultShellButton);
  addHelpId(defaultShellButton,-1);

  
  /* ---------- Creating Widget:    defaultShellMenu -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "radioBehavior", 1); n++;


  /* Creating Widget */
  defaultShellMenu = XmCreatePulldownMenu( parent, "defaultShellMenu", args, n);
  
  addQuickHelp(defaultShellMenu);
  addHelpId(defaultShellMenu,-1);

  
  /* ---------- Creating Widget:    applicationShellButton -------------- */
  parent = defaultShellMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  applicationShellButton = XtCreateWidget("applicationShellButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(applicationShellButton);
  
  addQuickHelp(applicationShellButton);
  addHelpId(applicationShellButton,-1);

  
  /* ---------- Creating Widget:    toplevelShellButton -------------- */
  parent = defaultShellMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  toplevelShellButton = XtCreateWidget("toplevelShellButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(toplevelShellButton);
  
  addQuickHelp(toplevelShellButton);
  addHelpId(toplevelShellButton,-1);

  
  /* ---------- Creating Widget:    transientShellButton -------------- */
  parent = defaultShellMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  transientShellButton = XtCreateWidget("transientShellButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(transientShellButton);
  
  addQuickHelp(transientShellButton);
  addHelpId(transientShellButton,-1);

  
  /* ---------- Creating Widget:    dialogShellButton -------------- */
  parent = defaultShellMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  dialogShellButton = XtCreateWidget("dialogShellButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(dialogShellButton);
  
  addQuickHelp(dialogShellButton);
  addHelpId(dialogShellButton,-1);

  
  /* ---------- Creating Widget:    viewDefaultsButton -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  viewDefaultsButton = XtCreateWidget("viewDefaultsButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(viewDefaultsButton);
  
  addQuickHelp(viewDefaultsButton);
  addHelpId(viewDefaultsButton,-1);

  
  /* ---------- Creating Widget:    defaultPublicButton -------------- */
  parent = OptionsMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  defaultPublicButton = XtCreateWidget("defaultPublicButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultPublicButton);
  
  addQuickHelp(defaultPublicButton);
  addHelpId(defaultPublicButton,-1);

  
  /* ---------- Creating Widget:    helpButton -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  helpButton = XtCreateWidget("helpButton", xmCascadeButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(helpButton);
  
  addQuickHelp(helpButton);
  addHelpId(helpButton,-1);

  
  /* ---------- Creating Widget:    HelpMenu -------------- */
  parent = MenuBar;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  HelpMenu = XmCreatePulldownMenu( parent, "HelpMenu", args, n);
  
  addQuickHelp(HelpMenu);
  addHelpId(HelpMenu,-1);

  
  /* ---------- Creating Widget:    HelpForHelp -------------- */
  parent = HelpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  HelpForHelp = XtCreateWidget("HelpForHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(HelpForHelp);
  
  addQuickHelp(HelpForHelp);
  addHelpId(HelpForHelp,-1);

  
  /* ---------- Creating Widget:    HelpOnContext -------------- */
  parent = HelpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  HelpOnContext = XtCreateWidget("HelpOnContext", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(HelpOnContext);
  
  addQuickHelp(HelpOnContext);
  addHelpId(HelpOnContext,-1);

  
  /* ---------- Creating Widget:    HelpTutorial -------------- */
  parent = HelpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  HelpTutorial = XtCreateWidget("HelpTutorial", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(HelpTutorial);
  
  addQuickHelp(HelpTutorial);
  addHelpId(HelpTutorial,-1);

  
  /* ---------- Creating Widget:    XmSeparator_1 -------------- */
  parent = HelpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  XmSeparator_1 = XtCreateWidget("XmSeparator_1", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmSeparator_1);
  
  addQuickHelp(XmSeparator_1);
  addHelpId(XmSeparator_1,-1);

  
  /* ---------- Creating Widget:    HelpProduct -------------- */
  parent = HelpMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  HelpProduct = XtCreateWidget("HelpProduct", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(HelpProduct);
  
  addQuickHelp(HelpProduct);
  addHelpId(HelpProduct,-1);

  
  /* ---------- Creating Widget:    OpenIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 5); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("OpenProject.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  OpenIcon = XtCreateWidget("OpenIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(OpenIcon);
  
  addQuickHelp(OpenIcon);
  addHelpId(OpenIcon,-1);

  
  /* ---------- Creating Widget:    SaveIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("SaveProject.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 2); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  SaveIcon = XtCreateWidget("SaveIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(SaveIcon);
  
  addQuickHelp(SaveIcon);
  addHelpId(SaveIcon,-1);

  
  /* ---------- Creating Widget:    PaletteIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 95); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("Templates.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 2); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  PaletteIcon = XtCreateWidget("PaletteIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PaletteIcon);
  
  addQuickHelp(PaletteIcon);
  addHelpId(PaletteIcon,-1);

  
  /* ---------- Creating Widget:    ProductIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 405); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("Logo_16.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ProductIcon = XtCreateWidget("ProductIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProductIcon);
  
  addQuickHelp(ProductIcon);
  addHelpId(ProductIcon,-1);

  
  /* ---------- Creating Widget:    seperateBelowToolBar -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 10); n++;
  XtSetArg(args[n], "width", 490); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 80); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  seperateBelowToolBar = XtCreateWidget("seperateBelowToolBar", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(seperateBelowToolBar);
  
  addQuickHelp(seperateBelowToolBar);
  addHelpId(seperateBelowToolBar,-1);

  
  /* ---------- Creating Widget:    WidgetPaletteIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 140); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("WidgetPalette.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 2); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  WidgetPaletteIcon = XtCreateWidget("WidgetPaletteIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(WidgetPaletteIcon);
  
  addQuickHelp(WidgetPaletteIcon);
  addHelpId(WidgetPaletteIcon,-1);

  
  /* ---------- Creating Widget:    XmPanedWindow_1 -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 342); n++;
  XtSetArg(args[n], "width", 490); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 90); n++;
  XtSetArg(args[n], "bottomAttachment", 3); n++;
  XtSetArg(args[n], "bottomOffset", 3); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  XmPanedWindow_1 = XtCreateWidget("XmPanedWindow_1", xmPanedWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmPanedWindow_1);
  
  addQuickHelp(XmPanedWindow_1);
  addHelpId(XmPanedWindow_1,-1);

  
  /* ---------- Creating Widget:    ViewForm -------------- */
  parent = XmPanedWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 130); n++;
  XtSetArg(args[n], "width", 484); n++;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "paneMinimum", 40); n++;


  /* Creating Widget */
  ViewForm = XtCreateWidget("ViewForm", xmFormWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ViewForm);
  
  addQuickHelp(ViewForm);
  addHelpId(ViewForm,-1);

  
  /* ---------- Creating Widget:    ViewLabel -------------- */
  parent = ViewForm;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 90); n++;
  XtSetArg(args[n], "x", 40); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  ViewLabel = XtCreateWidget("ViewLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ViewLabel);
  
  addQuickHelp(ViewLabel);
  addHelpId(ViewLabel,-1);

  
  /* ---------- Creating Widget:    MessageForm -------------- */
  parent = XmPanedWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 198); n++;
  XtSetArg(args[n], "width", 484); n++;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "paneMinimum", 80); n++;


  /* Creating Widget */
  MessageForm = XtCreateWidget("MessageForm", xmFormWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(MessageForm);
  
  addQuickHelp(MessageForm);
  addHelpId(MessageForm,-1);

  
  /* ---------- Creating Widget:    MessageLabel -------------- */
  parent = MessageForm;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 90); n++;
  XtSetArg(args[n], "x", 40); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "bottomAttachment", 3); n++;
  XtSetArg(args[n], "bottomOffset", 5); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  MessageLabel = XtCreateWidget("MessageLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(MessageLabel);
  
  addQuickHelp(MessageLabel);
  addHelpId(MessageLabel,-1);

  
  /* ---------- Creating Widget:    XmScrolledWindow_1 -------------- */
  parent = MessageForm;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 172); n++;
  XtSetArg(args[n], "width", 484); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 35); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 3); n++;


  /* Creating Widget */
  XmScrolledWindow_1 = XtCreateWidget("XmScrolledWindow_1", xmScrolledWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmScrolledWindow_1);
  
  addQuickHelp(XmScrolledWindow_1);
  addHelpId(XmScrolledWindow_1,-1);

  
  /* ---------- Creating Widget:    XmText_1 -------------- */
  parent = XmScrolledWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 155); n++;
  XtSetArg(args[n], "width", 467); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "editMode", 0); n++;
  XtSetArg(args[n], "editable", 0); n++;


  /* Creating Widget */
  XmText_1 = XtCreateWidget("XmText_1", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmText_1);
  
  addQuickHelp(XmText_1);
  addHelpId(XmText_1,-1);

  
  /* ---------- Creating Widget:    MessageClearButton -------------- */
  parent = MessageForm;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 320); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 1); n++;


  /* Creating Widget */
  MessageClearButton = XtCreateWidget("MessageClearButton", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(MessageClearButton);
  
  addQuickHelp(MessageClearButton);
  addHelpId(MessageClearButton,-1);

  
  /* ---------- Creating Widget:    quickHelp -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 443); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 140); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "marginHeight", 0); n++;
  XtSetArg(args[n], "marginWidth", 0); n++;
  XtSetArg(args[n], "recomputeSize", 0); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 3); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;


  /* Creating Widget */
  quickHelp = XtCreateWidget("quickHelp", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(quickHelp);
  
  addQuickHelp(quickHelp);
  addHelpId(quickHelp,-1);

  
  /* ---------- Creating Widget:    TestModeToggle -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 41); n++;
  XtSetArg(args[n], "width", 100); n++;
  XtSetArg(args[n], "x", 290); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "indicatorSize", 20); n++;
  XtSetArg(args[n], "bottomAttachment", 3); n++;
  XtSetArg(args[n], "rightAttachment", 3); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  TestModeToggle = XtCreateWidget("TestModeToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(TestModeToggle);
  
  addQuickHelp(TestModeToggle);
  addHelpId(TestModeToggle,-1);

  
  /* ---------- Creating Widget:    ProductInfoDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 170); n++;
  XtSetArg(args[n], "width", 270); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "noResize", 1); n++;


  /* Creating Widget */
  ProductInfoDialog = XmCreateFormDialog( parent, "ProductInfoDialog", args, n);
  
  addQuickHelp(ProductInfoDialog);
  addHelpId(ProductInfoDialog,-1);

  
  /* ---------- Creating Widget:    PIClose -------------- */
  parent = ProductInfoDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 90); n++;
  XtSetArg(args[n], "x", 90); n++;
  XtSetArg(args[n], "y", 160); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -45); n++;
  XtSetArg(args[n], "leftPosition", 50); n++;


  /* Creating Widget */
  PIClose = XtCreateWidget("PIClose", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PIClose);
  
  addQuickHelp(PIClose);
  addHelpId(PIClose,-1);

  
  /* ---------- Creating Widget:    ProductInfoLabel -------------- */
  parent = ProductInfoDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 15); n++;


  /* Creating Widget */
  ProductInfoLabel = XtCreateWidget("ProductInfoLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProductInfoLabel);
  
  addQuickHelp(ProductInfoLabel);
  addHelpId(ProductInfoLabel,-1);

  
  /* ---------- Creating Widget:    label_2 -------------- */
  parent = ProductInfoDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 18); n++;
  XtSetArg(args[n], "width", 250); n++;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 108); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 20); n++;


  /* Creating Widget */
  label_2 = XtCreateWidget("label_2", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_2);
  
  addQuickHelp(label_2);
  addHelpId(label_2,-1);

  
  /* ---------- Creating Widget:    ProjectNewDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 110); n++;
  XtSetArg(args[n], "width", 340); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "autoUnmanage", 0); n++;
  XtSetArg(args[n], "noResize", 1); n++;


  /* Creating Widget */
  ProjectNewDialog = XmCreateFormDialog( parent, "ProjectNewDialog", args, n);
  
  addQuickHelp(ProjectNewDialog);
  addHelpId(ProjectNewDialog,2);

  
  /* ---------- Creating Widget:    ProjectNewLabel -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Project Name:")); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  ProjectNewLabel = XtCreateWidget("ProjectNewLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewLabel);
  
  addQuickHelp(ProjectNewLabel);
  addHelpId(ProjectNewLabel,-1);

  
  /* ---------- Creating Widget:    ProjectNewText -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 181); n++;
  XtSetArg(args[n], "x", 160); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  ProjectNewText = XtCreateWidget("ProjectNewText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewText);
  
  addQuickHelp(ProjectNewText);
  addHelpId(ProjectNewText,-1);

  
  /* ---------- Creating Widget:    ProjectNewOk -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 80); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Create")); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -46); n++;
  XtSetArg(args[n], "leftPosition", 20); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ProjectNewOk = XtCreateWidget("ProjectNewOk", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewOk);
  
  addQuickHelp(ProjectNewOk);
  addHelpId(ProjectNewOk,-1);

  
  /* ---------- Creating Widget:    ProjectNewCancel -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 125); n++;
  XtSetArg(args[n], "y", 80); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Cancel")); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -46); n++;
  XtSetArg(args[n], "leftPosition", 50); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ProjectNewCancel = XtCreateWidget("ProjectNewCancel", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewCancel);
  
  addQuickHelp(ProjectNewCancel);
  addHelpId(ProjectNewCancel,-1);

  
  /* ---------- Creating Widget:    ProjectNewHelp -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 210); n++;
  XtSetArg(args[n], "y", 80); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Help")); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -46); n++;
  XtSetArg(args[n], "leftPosition", 80); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ProjectNewHelp = XtCreateWidget("ProjectNewHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewHelp);
  
  addQuickHelp(ProjectNewHelp);
  addHelpId(ProjectNewHelp,-1);

  
  /* ---------- Creating Widget:    ProjectNewSeparator -------------- */
  parent = ProjectNewDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 10); n++;
  XtSetArg(args[n], "width", 338); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ProjectNewSeparator = XtCreateWidget("ProjectNewSeparator", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ProjectNewSeparator);
  
  addQuickHelp(ProjectNewSeparator);
  addHelpId(ProjectNewSeparator,-1);

  
  /* ---------- Creating Widget:    ProjectSettingsDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 270); n++;
  XtSetArg(args[n], "width", 350); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "autoUnmanage", 0); n++;


  /* Creating Widget */
  ProjectSettingsDialog = XmCreateFormDialog( parent, "ProjectSettingsDialog", args, n);
  
  addQuickHelp(ProjectSettingsDialog);
  addHelpId(ProjectSettingsDialog,15);

  
  /* ---------- Creating Widget:    PSNameLabel -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 110); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSNameLabel = XtCreateWidget("PSNameLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSNameLabel);
  
  addQuickHelp(PSNameLabel);
  addHelpId(PSNameLabel,-1);

  
  /* ---------- Creating Widget:    PSNameText -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 215); n++;
  XtSetArg(args[n], "x", 90); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSNameText = XtCreateWidget("PSNameText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSNameText);
  
  addQuickHelp(PSNameText);
  addHelpId(PSNameText,-1);

  
  /* ---------- Creating Widget:    PSPaletteLabel -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 110); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 50); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSPaletteLabel = XtCreateWidget("PSPaletteLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSPaletteLabel);
  
  addQuickHelp(PSPaletteLabel);
  addHelpId(PSPaletteLabel,-1);

  
  /* ---------- Creating Widget:    PSPaletteText -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 31); n++;
  XtSetArg(args[n], "width", 215); n++;
  XtSetArg(args[n], "x", 125); n++;
  XtSetArg(args[n], "y", 50); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSPaletteText = XtCreateWidget("PSPaletteText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSPaletteText);
  
  addQuickHelp(PSPaletteText);
  addHelpId(PSPaletteText,-1);

  
  /* ---------- Creating Widget:    PSLangMenu -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  PSLangMenu = XmCreatePulldownMenu( parent, "PSLangMenu", args, n);
  
  addQuickHelp(PSLangMenu);
  addHelpId(PSLangMenu,-1);

  
  /* ---------- Creating Widget:    PSLangOptionMenu -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 130); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "rightAttachment", 0); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSLangOptionMenu = XmCreateOptionMenu( parent, "PSLangOptionMenu", args, n);
  
  /* Manage the Widget */
  XtManageChild(PSLangOptionMenu);
  
  addQuickHelp(PSLangOptionMenu);
  addHelpId(PSLangOptionMenu,-1);

  
  /* ---------- Creating Widget:    XmSeparator_4 -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 7); n++;
  XtSetArg(args[n], "width", 348); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 150); n++;
  XtSetArg(args[n], "bottomAttachment", 3); n++;
  XtSetArg(args[n], "bottomOffset", 5); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  XmSeparator_4 = XtCreateWidget("XmSeparator_4", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(XmSeparator_4);
  
  addQuickHelp(XmSeparator_4);
  addHelpId(XmSeparator_4,-1);

  
  /* ---------- Creating Widget:    PSStartLabel -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 110); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 130); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSStartLabel = XtCreateWidget("PSStartLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSStartLabel);
  
  addQuickHelp(PSStartLabel);
  addHelpId(PSStartLabel,-1);

  
  /* ---------- Creating Widget:    PCStartText -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "width", 215); n++;
  XtSetArg(args[n], "x", 110); n++;
  XtSetArg(args[n], "y", 100); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PCStartText = XtCreateWidget("PCStartText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PCStartText);
  
  addQuickHelp(PCStartText);
  addHelpId(PCStartText,-1);

  
  /* ---------- Creating Widget:    PSOk -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 160); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -48); n++;
  XtSetArg(args[n], "leftPosition", 20); n++;


  /* Creating Widget */
  PSOk = XtCreateWidget("PSOk", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSOk);
  
  addQuickHelp(PSOk);
  addHelpId(PSOk,-1);

  
  /* ---------- Creating Widget:    PSCancel -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 120); n++;
  XtSetArg(args[n], "y", 160); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -48); n++;
  XtSetArg(args[n], "leftPosition", 50); n++;


  /* Creating Widget */
  PSCancel = XtCreateWidget("PSCancel", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSCancel);
  
  addQuickHelp(PSCancel);
  addHelpId(PSCancel,-1);

  
  /* ---------- Creating Widget:    PSHelp -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 90); n++;
  XtSetArg(args[n], "x", 180); n++;
  XtSetArg(args[n], "y", 160); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -48); n++;
  XtSetArg(args[n], "leftPosition", 80); n++;


  /* Creating Widget */
  PSHelp = XtCreateWidget("PSHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSHelp);
  
  addQuickHelp(PSHelp);
  addHelpId(PSHelp,-1);

  
  /* ---------- Creating Widget:    PSSourceDirLabel -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 110); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 90); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSSourceDirLabel = XtCreateWidget("PSSourceDirLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSSourceDirLabel);
  
  addQuickHelp(PSSourceDirLabel);
  addHelpId(PSSourceDirLabel,-1);

  
  /* ---------- Creating Widget:    PSSourceDirText -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 31); n++;
  XtSetArg(args[n], "width", 180); n++;
  XtSetArg(args[n], "x", 125); n++;
  XtSetArg(args[n], "y", 90); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 5); n++;
  XtSetArg(args[n], "rightAttachment", 3); n++;
  XtSetArg(args[n], "rightOffset", 5); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSSourceDirText = XtCreateWidget("PSSourceDirText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSSourceDirText);
  
  addQuickHelp(PSSourceDirText);
  addHelpId(PSSourceDirText,-1);

  
  /* ---------- Creating Widget:    PSPaletteFiles -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 30); n++;
  XtSetArg(args[n], "x", 310); n++;
  XtSetArg(args[n], "y", 50); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSPaletteFiles = XtCreateWidget("PSPaletteFiles", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  addQuickHelp(PSPaletteFiles);
  addHelpId(PSPaletteFiles,-1);

  
  /* ---------- Creating Widget:    PSSourceDirFiles -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 30); n++;
  XtSetArg(args[n], "x", 310); n++;
  XtSetArg(args[n], "y", 90); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  PSSourceDirFiles = XtCreateWidget("PSSourceDirFiles", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(PSSourceDirFiles);
  
  addQuickHelp(PSSourceDirFiles);
  addHelpId(PSSourceDirFiles,-1);

  
  /* ---------- Creating Widget:    PSPaletteFileSB -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 210); n++;
  XtSetArg(args[n], "y", 170); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "dirMask", RES_CONVERT("XmString", "*.pal")); n++;


  /* Creating Widget */
  PSPaletteFileSB = XmCreateFileSelectionDialog( parent, "PSPaletteFileSB", args, n);
  
  addQuickHelp(PSPaletteFileSB);
  addHelpId(PSPaletteFileSB,-1);

  
  /* ---------- Creating Widget:    PSSourceDirFileSB -------------- */
  parent = ProjectSettingsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 250); n++;
  XtSetArg(args[n], "y", 180); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "fileTypeMask", 1); n++;


  /* Creating Widget */
  PSSourceDirFileSB = XmCreateFileSelectionDialog( parent, "PSSourceDirFileSB", args, n);
  
  addQuickHelp(PSSourceDirFileSB);
  addHelpId(PSSourceDirFileSB,-1);

  
  /* ---------- Creating Widget:    GridDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 200); n++;
  XtSetArg(args[n], "width", 330); n++;
  XtSetArg(args[n], "x", 170); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "autoUnmanage", 0); n++;
  XtSetArg(args[n], "noResize", 1); n++;


  /* Creating Widget */
  GridDialog = XmCreateFormDialog( parent, "GridDialog", args, n);
  
  addQuickHelp(GridDialog);
  addHelpId(GridDialog,18);

  
  /* ---------- Creating Widget:    GridOk -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 140); n++;


  /* Creating Widget */
  GridOk = XtCreateWidget("GridOk", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridOk);
  
  addQuickHelp(GridOk);
  addHelpId(GridOk,-1);

  
  /* ---------- Creating Widget:    GridCancel -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 120); n++;
  XtSetArg(args[n], "y", 140); n++;


  /* Creating Widget */
  GridCancel = XtCreateWidget("GridCancel", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridCancel);
  
  addQuickHelp(GridCancel);
  addHelpId(GridCancel,-1);

  
  /* ---------- Creating Widget:    GridHelp -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 220); n++;
  XtSetArg(args[n], "y", 140); n++;


  /* Creating Widget */
  GridHelp = XtCreateWidget("GridHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridHelp);
  
  addQuickHelp(GridHelp);
  addHelpId(GridHelp,-1);

  
  /* ---------- Creating Widget:    GridSeparator -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 20); n++;
  XtSetArg(args[n], "width", 320); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 110); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 1); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 1); n++;


  /* Creating Widget */
  GridSeparator = XtCreateWidget("GridSeparator", xmSeparatorWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridSeparator);
  
  addQuickHelp(GridSeparator);
  addHelpId(GridSeparator,-1);

  
  /* ---------- Creating Widget:    GridRadioBox -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "borderWidth", 1); n++;
  XtSetArg(args[n], "height", 31); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "orientation", 2); n++;
  XtSetArg(args[n], "packing", 2); n++;


  /* Creating Widget */
  GridRadioBox = XmCreateRadioBox( parent, "GridRadioBox", args, n);
  
  /* Manage the Widget */
  XtManageChild(GridRadioBox);
  
  addQuickHelp(GridRadioBox);
  addHelpId(GridRadioBox,-1);

  
  /* ---------- Creating Widget:    GridModeOn -------------- */
  parent = GridRadioBox;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  GridModeOn = XtCreateWidget("GridModeOn", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridModeOn);
  
  addQuickHelp(GridModeOn);
  addHelpId(GridModeOn,-1);

  
  /* ---------- Creating Widget:    GridModeOff -------------- */
  parent = GridRadioBox;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  GridModeOff = XtCreateWidget("GridModeOff", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridModeOff);
  
  addQuickHelp(GridModeOff);
  addHelpId(GridModeOff,-1);

  
  /* ---------- Creating Widget:    GridValueLabel -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 100); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 60); n++;
  XtSetArg(args[n], "alignment", 0); n++;


  /* Creating Widget */
  GridValueLabel = XtCreateWidget("GridValueLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridValueLabel);
  
  addQuickHelp(GridValueLabel);
  addHelpId(GridValueLabel,-1);

  
  /* ---------- Creating Widget:    GridValue -------------- */
  parent = GridDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 170); n++;
  XtSetArg(args[n], "x", 130); n++;
  XtSetArg(args[n], "y", 50); n++;
  XtSetArg(args[n], "maximum", 50); n++;
  XtSetArg(args[n], "minimum", 1); n++;
  XtSetArg(args[n], "orientation", 2); n++;
  XtSetArg(args[n], "processingDirection", 3); n++;
  XtSetArg(args[n], "showValue", 1); n++;
  XtSetArg(args[n], "value", 10); n++;


  /* Creating Widget */
  GridValue = XtCreateWidget("GridValue", xmScaleWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(GridValue);
  
  addQuickHelp(GridValue);
  addHelpId(GridValue,-1);

  
  /* ---------- Creating Widget:    saveProjectDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 170); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "dirMask", RES_CONVERT("XmString", "*.prj")); n++;


  /* Creating Widget */
  saveProjectDialog = XmCreateFileSelectionDialog( parent, "saveProjectDialog", args, n);
  
  addQuickHelp(saveProjectDialog);
  addHelpId(saveProjectDialog,21);

  
  /* ---------- Creating Widget:    loadProjectDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 170); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "dirMask", RES_CONVERT("XmString", "*.prj")); n++;


  /* Creating Widget */
  loadProjectDialog = XmCreateFileSelectionDialog( parent, "loadProjectDialog", args, n);
  
  addQuickHelp(loadProjectDialog);
  addHelpId(loadProjectDialog,21);

  
  /* ---------- Creating Widget:    loadInterfaceDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 170); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "dirMask", RES_CONVERT("XmString", "*.i")); n++;


  /* Creating Widget */
  loadInterfaceDialog = XmCreateFileSelectionDialog( parent, "loadInterfaceDialog", args, n);
  
  addQuickHelp(loadInterfaceDialog);
  addHelpId(loadInterfaceDialog,21);

  
  /* ---------- Creating Widget:    overwriteDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 300); n++;
  XtSetArg(args[n], "width", 540); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "autoUnmanage", 0); n++;
  XtSetArg(args[n], "resizePolicy", 1); n++;


  /* Creating Widget */
  overwriteDialog = XmCreateFormDialog( parent, "overwriteDialog", args, n);
  
  addQuickHelp(overwriteDialog);
  addHelpId(overwriteDialog,19);

  
  /* ---------- Creating Widget:    owOk -------------- */
  parent = overwriteDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 32); n++;
  XtSetArg(args[n], "width", 90); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 258); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;


  /* Creating Widget */
  owOk = XtCreateWidget("owOk", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(owOk);
  
  addQuickHelp(owOk);
  addHelpId(owOk,-1);

  
  /* ---------- Creating Widget:    owCancel -------------- */
  parent = overwriteDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 110); n++;
  XtSetArg(args[n], "y", 250); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 5); n++;
  XtSetArg(args[n], "leftOffset", -45); n++;
  XtSetArg(args[n], "leftPosition", 50); n++;


  /* Creating Widget */
  owCancel = XtCreateWidget("owCancel", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(owCancel);
  
  addQuickHelp(owCancel);
  addHelpId(owCancel,-1);

  
  /* ---------- Creating Widget:    owHelp -------------- */
  parent = overwriteDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 30); n++;
  XtSetArg(args[n], "width", 80); n++;
  XtSetArg(args[n], "x", 210); n++;
  XtSetArg(args[n], "y", 250); n++;
  XtSetArg(args[n], "bottomAttachment", 1); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;


  /* Creating Widget */
  owHelp = XtCreateWidget("owHelp", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(owHelp);
  
  addQuickHelp(owHelp);
  addHelpId(owHelp,-1);

  
  /* ---------- Creating Widget:    scrolledWindow_1 -------------- */
  parent = overwriteDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 226); n++;
  XtSetArg(args[n], "width", 520); n++;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 70); n++;
  XtSetArg(args[n], "scrollBarDisplayPolicy", 0); n++;
  XtSetArg(args[n], "bottomAttachment", 3); n++;
  XtSetArg(args[n], "bottomOffset", 10); n++;
  XtSetArg(args[n], "leftAttachment", 1); n++;
  XtSetArg(args[n], "leftOffset", 10); n++;
  XtSetArg(args[n], "rightAttachment", 1); n++;
  XtSetArg(args[n], "rightOffset", 10); n++;
  XtSetArg(args[n], "topAttachment", 1); n++;
  XtSetArg(args[n], "topOffset", 10); n++;


  /* Creating Widget */
  scrolledWindow_1 = XtCreateWidget("scrolledWindow_1", xmScrolledWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(scrolledWindow_1);
  
  addQuickHelp(scrolledWindow_1);
  addHelpId(scrolledWindow_1,-1);

  
  /* ---------- Creating Widget:    overwriteFilesList -------------- */
  parent = scrolledWindow_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 226); n++;
  XtSetArg(args[n], "width", 520); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "listSizePolicy", 0); n++;
  XtSetArg(args[n], "scrollBarDisplayPolicy", 1); n++;
  XtSetArg(args[n], "selectionPolicy", 1); n++;


  /* Creating Widget */
  overwriteFilesList = XtCreateWidget("overwriteFilesList", xmListWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(overwriteFilesList);
  
  addQuickHelp(overwriteFilesList);
  addHelpId(overwriteFilesList,-1);

  
  /* ---------- Creating Widget:    ExecIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 140); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("Execute.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 2); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  ExecIcon = XtCreateWidget("ExecIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ExecIcon);
  
  addQuickHelp(ExecIcon);
  addHelpId(ExecIcon,-1);

  
  /* ---------- Creating Widget:    openTemplatesDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 200); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "autoUnmanage", 1); n++;
  XtSetArg(args[n], "dialogTitle", RES_CONVERT("XmString", "Open Template File")); n++;
  XtSetArg(args[n], "dirMask", RES_CONVERT("XmString", "*.pal")); n++;


  /* Creating Widget */
  openTemplatesDialog = XmCreateFileSelectionDialog( parent, "openTemplatesDialog", args, n);
  
  addQuickHelp(openTemplatesDialog);
  addHelpId(openTemplatesDialog,21);

  
  /* ---------- Creating Widget:    quickStartDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  quickStartDialog = XmCreateQuestionDialog( parent, "quickStartDialog", args, n);
  
  addQuickHelp(quickStartDialog);
  addHelpId(quickStartDialog,42);

  
  /* ---------- Creating Widget:    xlCellTable_1 -------------- */
  parent = quickStartDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 100); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  xlCellTable_1 = XtCreateWidget("xlCellTable_1", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_1);
  
  addQuickHelp(xlCellTable_1);
  addHelpId(xlCellTable_1,-1);

  
  /* ---------- Creating Widget:    qsOpenButton -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 30); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "indicatorType", 2); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Open Project:")); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  qsOpenButton = XtCreateWidget("qsOpenButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsOpenButton);
  
  addQuickHelp(qsOpenButton);
  addHelpId(qsOpenButton,-1);

  
  /* ---------- Creating Widget:    qsNewButton -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "indicatorType", 2); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  qsNewButton = XtCreateWidget("qsNewButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsNewButton);
  
  addQuickHelp(qsNewButton);
  addHelpId(qsNewButton,-1);

  
  /* ---------- Creating Widget:    qsRecoverButton -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "alignment", 0); n++;
  XtSetArg(args[n], "indicatorType", 2); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;


  /* Creating Widget */
  qsRecoverButton = XtCreateWidget("qsRecoverButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsRecoverButton);
  
  addQuickHelp(qsRecoverButton);
  addHelpId(qsRecoverButton,-1);

  
  /* ---------- Creating Widget:    qsOpenText -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 80); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "resizeWidth", 1); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;


  /* Creating Widget */
  qsOpenText = XtCreateWidget("qsOpenText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsOpenText);
  
  addQuickHelp(qsOpenText);
  addHelpId(qsOpenText,-1);

  
  /* ---------- Creating Widget:    qsNewText -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 80); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "resizeWidth", 1); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 1); n++;


  /* Creating Widget */
  qsNewText = XtCreateWidget("qsNewText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsNewText);
  
  addQuickHelp(qsNewText);
  addHelpId(qsNewText,-1);

  
  /* ---------- Creating Widget:    qsRecoverText -------------- */
  parent = xlCellTable_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 80); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "resizeWidth", 1); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 2); n++;


  /* Creating Widget */
  qsRecoverText = XtCreateWidget("qsRecoverText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(qsRecoverText);
  
  addQuickHelp(qsRecoverText);
  addHelpId(qsRecoverText,-1);

  
  /* ---------- Creating Widget:    generateIcon -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 40); n++;
  XtSetArg(args[n], "width", 40); n++;
  XtSetArg(args[n], "x", 220); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("Generate.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "leftAttachment", 3); n++;
  XtSetArg(args[n], "leftOffset", 2); n++;
  XtSetArg(args[n], "topAttachment", 3); n++;
  XtSetArg(args[n], "topOffset", 5); n++;


  /* Creating Widget */
  generateIcon = XtCreateWidget("generateIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(generateIcon);
  
  addQuickHelp(generateIcon);
  addHelpId(generateIcon,-1);

  
  /* ---------- Creating Widget:    viewDefaultsDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "messageString", RES_CONVERT("XmString", "Define default settings of views.")); n++;


  /* Creating Widget */
  viewDefaultsDialog = XmCreateMessageDialog( parent, "viewDefaultsDialog", args, n);
  
  addQuickHelp(viewDefaultsDialog);
  addHelpId(viewDefaultsDialog,39);

  
  /* ---------- Creating Widget:    rowColumn_2 -------------- */
  parent = viewDefaultsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 100); n++;
  XtSetArg(args[n], "width", 100); n++;
  XtSetArg(args[n], "x", 20); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "numColumns", 2); n++;
  XtSetArg(args[n], "packing", 2); n++;


  /* Creating Widget */
  rowColumn_2 = XtCreateWidget("rowColumn_2", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_2);
  
  addQuickHelp(rowColumn_2);
  addHelpId(rowColumn_2,-1);

  
  /* ---------- Creating Widget:    widBrowseViewFrame -------------- */
  parent = rowColumn_2;

  /* Building resource list */
  n = 0;


  /* Creating Widget */
  widBrowseViewFrame = XtCreateWidget("widBrowseViewFrame", xmFrameWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(widBrowseViewFrame);
  
  addQuickHelp(widBrowseViewFrame);
  addHelpId(widBrowseViewFrame,-1);

  
  /* ---------- Creating Widget:    label_3 -------------- */
  parent = widBrowseViewFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "childType", 2); n++;


  /* Creating Widget */
  label_3 = XtCreateWidget("label_3", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_3);
  
  addQuickHelp(label_3);
  addHelpId(label_3,-1);

  
  /* ---------- Creating Widget:    rowColumn_3 -------------- */
  parent = widBrowseViewFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 119); n++;
  XtSetArg(args[n], "width", 166); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  rowColumn_3 = XtCreateWidget("rowColumn_3", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_3);
  
  addQuickHelp(rowColumn_3);
  addHelpId(rowColumn_3,-1);

  
  /* ---------- Creating Widget:    browserSmallIconToggle -------------- */
  parent = rowColumn_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  browserSmallIconToggle = XtCreateWidget("browserSmallIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(browserSmallIconToggle);
  
  addQuickHelp(browserSmallIconToggle);
  addHelpId(browserSmallIconToggle,-1);

  
  /* ---------- Creating Widget:    browserWithIconToggle -------------- */
  parent = rowColumn_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  browserWithIconToggle = XtCreateWidget("browserWithIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(browserWithIconToggle);
  
  addQuickHelp(browserWithIconToggle);
  addHelpId(browserWithIconToggle,-1);

  
  /* ---------- Creating Widget:    browserWithClassNameToggle -------------- */
  parent = rowColumn_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  browserWithClassNameToggle = XtCreateWidget("browserWithClassNameToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(browserWithClassNameToggle);
  
  addQuickHelp(browserWithClassNameToggle);
  addHelpId(browserWithClassNameToggle,-1);

  
  /* ---------- Creating Widget:    browserAutomaticLoadToggle -------------- */
  parent = rowColumn_3;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  browserAutomaticLoadToggle = XtCreateWidget("browserAutomaticLoadToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(browserAutomaticLoadToggle);
  
  addQuickHelp(browserAutomaticLoadToggle);
  addHelpId(browserAutomaticLoadToggle,-1);

  
  /* ---------- Creating Widget:    menuEditFrame -------------- */
  parent = rowColumn_2;

  /* Building resource list */
  n = 0;


  /* Creating Widget */
  menuEditFrame = XtCreateWidget("menuEditFrame", xmFrameWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditFrame);
  
  addQuickHelp(menuEditFrame);
  addHelpId(menuEditFrame,-1);

  
  /* ---------- Creating Widget:    label_3_1 -------------- */
  parent = menuEditFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "childType", 2); n++;


  /* Creating Widget */
  label_3_1 = XtCreateWidget("label_3_1", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_3_1);
  
  addQuickHelp(label_3_1);
  addHelpId(label_3_1,-1);

  
  /* ---------- Creating Widget:    rowColumn_3_1 -------------- */
  parent = menuEditFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 119); n++;
  XtSetArg(args[n], "width", 166); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  rowColumn_3_1 = XtCreateWidget("rowColumn_3_1", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_3_1);
  
  addQuickHelp(rowColumn_3_1);
  addHelpId(rowColumn_3_1,-1);

  
  /* ---------- Creating Widget:    menuEditSmallIconToggle -------------- */
  parent = rowColumn_3_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  menuEditSmallIconToggle = XtCreateWidget("menuEditSmallIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditSmallIconToggle);
  
  addQuickHelp(menuEditSmallIconToggle);
  addHelpId(menuEditSmallIconToggle,-1);

  
  /* ---------- Creating Widget:    menuEditWithIconToggle -------------- */
  parent = rowColumn_3_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  menuEditWithIconToggle = XtCreateWidget("menuEditWithIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditWithIconToggle);
  
  addQuickHelp(menuEditWithIconToggle);
  addHelpId(menuEditWithIconToggle,-1);

  
  /* ---------- Creating Widget:    menuEditWithClassNameToggle -------------- */
  parent = rowColumn_3_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  menuEditWithClassNameToggle = XtCreateWidget("menuEditWithClassNameToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditWithClassNameToggle);
  
  addQuickHelp(menuEditWithClassNameToggle);
  addHelpId(menuEditWithClassNameToggle,-1);

  
  /* ---------- Creating Widget:    menuEditAutomaticLoadToggle -------------- */
  parent = rowColumn_3_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  menuEditAutomaticLoadToggle = XtCreateWidget("menuEditAutomaticLoadToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(menuEditAutomaticLoadToggle);
  
  addQuickHelp(menuEditAutomaticLoadToggle);
  addHelpId(menuEditAutomaticLoadToggle,-1);

  
  /* ---------- Creating Widget:    attachmentEditorFrame -------------- */
  parent = rowColumn_2;

  /* Building resource list */
  n = 0;


  /* Creating Widget */
  attachmentEditorFrame = XtCreateWidget("attachmentEditorFrame", xmFrameWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(attachmentEditorFrame);
  
  addQuickHelp(attachmentEditorFrame);
  addHelpId(attachmentEditorFrame,-1);

  
  /* ---------- Creating Widget:    label_3_1_1 -------------- */
  parent = attachmentEditorFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "childType", 2); n++;


  /* Creating Widget */
  label_3_1_1 = XtCreateWidget("label_3_1_1", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_3_1_1);
  
  addQuickHelp(label_3_1_1);
  addHelpId(label_3_1_1,-1);

  
  /* ---------- Creating Widget:    rowColumn_3_1_1 -------------- */
  parent = attachmentEditorFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 119); n++;
  XtSetArg(args[n], "width", 180); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  rowColumn_3_1_1 = XtCreateWidget("rowColumn_3_1_1", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_3_1_1);
  
  addQuickHelp(rowColumn_3_1_1);
  addHelpId(rowColumn_3_1_1,-1);

  
  /* ---------- Creating Widget:    aeSmallIconToggle -------------- */
  parent = rowColumn_3_1_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  aeSmallIconToggle = XtCreateWidget("aeSmallIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(aeSmallIconToggle);
  
  addQuickHelp(aeSmallIconToggle);
  addHelpId(aeSmallIconToggle,-1);

  
  /* ---------- Creating Widget:    aeWithIconToggle -------------- */
  parent = rowColumn_3_1_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  aeWithIconToggle = XtCreateWidget("aeWithIconToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(aeWithIconToggle);
  
  addQuickHelp(aeWithIconToggle);
  addHelpId(aeWithIconToggle,-1);

  
  /* ---------- Creating Widget:    aeWithClassNameToggle -------------- */
  parent = rowColumn_3_1_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  aeWithClassNameToggle = XtCreateWidget("aeWithClassNameToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(aeWithClassNameToggle);
  
  addQuickHelp(aeWithClassNameToggle);
  addHelpId(aeWithClassNameToggle,-1);

  
  /* ---------- Creating Widget:    aeAutomaticLoadToggle -------------- */
  parent = rowColumn_3_1_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  aeAutomaticLoadToggle = XtCreateWidget("aeAutomaticLoadToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(aeAutomaticLoadToggle);
  
  addQuickHelp(aeAutomaticLoadToggle);
  addHelpId(aeAutomaticLoadToggle,-1);

  
  /* ---------- Creating Widget:    resourceEditorFrame -------------- */
  parent = rowColumn_2;

  /* Building resource list */
  n = 0;


  /* Creating Widget */
  resourceEditorFrame = XtCreateWidget("resourceEditorFrame", xmFrameWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(resourceEditorFrame);
  
  addQuickHelp(resourceEditorFrame);
  addHelpId(resourceEditorFrame,-1);

  
  /* ---------- Creating Widget:    label_3_2 -------------- */
  parent = resourceEditorFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "childType", 2); n++;


  /* Creating Widget */
  label_3_2 = XtCreateWidget("label_3_2", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_3_2);
  
  addQuickHelp(label_3_2);
  addHelpId(label_3_2,-1);

  
  /* ---------- Creating Widget:    rowColumn_3_2 -------------- */
  parent = resourceEditorFrame;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 119); n++;
  XtSetArg(args[n], "width", 180); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  rowColumn_3_2 = XtCreateWidget("rowColumn_3_2", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_3_2);
  
  addQuickHelp(rowColumn_3_2);
  addHelpId(rowColumn_3_2,-1);

  
  /* ---------- Creating Widget:    editorAutomaticLoadToggle -------------- */
  parent = rowColumn_3_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  editorAutomaticLoadToggle = XtCreateWidget("editorAutomaticLoadToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(editorAutomaticLoadToggle);
  
  addQuickHelp(editorAutomaticLoadToggle);
  addHelpId(editorAutomaticLoadToggle,-1);

  
  /* ---------- Creating Widget:    defaultViewOptionMenu -------------- */
  parent = rowColumn_3_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 30); n++;
  XtSetArg(args[n], "y", 40); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Default View:")); n++;


  /* Creating Widget */
  defaultViewOptionMenu = XmCreateOptionMenu( parent, "defaultViewOptionMenu", args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultViewOptionMenu);
  
  addQuickHelp(defaultViewOptionMenu);
  addHelpId(defaultViewOptionMenu,-1);

  
  /* ---------- Creating Widget:    defaultViewMenu -------------- */
  parent = defaultViewOptionMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 60); n++;
  XtSetArg(args[n], "y", 10); n++;


  /* Creating Widget */
  defaultViewMenu = XmCreatePulldownMenu( parent, "defaultViewMenu", args, n);
  
  addQuickHelp(defaultViewMenu);
  addHelpId(defaultViewMenu,-1);

  
  /* ---------- Creating Widget:    coreView -------------- */
  parent = defaultViewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  coreView = XtCreateWidget("coreView", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(coreView);
  
  addQuickHelp(coreView);
  addHelpId(coreView,-1);

  
  /* ---------- Creating Widget:    specificView -------------- */
  parent = defaultViewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  specificView = XtCreateWidget("specificView", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(specificView);
  
  addQuickHelp(specificView);
  addHelpId(specificView,-1);

  
  /* ---------- Creating Widget:    constraintView -------------- */
  parent = defaultViewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  constraintView = XtCreateWidget("constraintView", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(constraintView);
  
  addQuickHelp(constraintView);
  addHelpId(constraintView,-1);

  
  /* ---------- Creating Widget:    callbackView -------------- */
  parent = defaultViewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  callbackView = XtCreateWidget("callbackView", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(callbackView);
  
  addQuickHelp(callbackView);
  addHelpId(callbackView,-1);

  
  /* ---------- Creating Widget:    extraView -------------- */
  parent = defaultViewMenu;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Extra")); n++;


  /* Creating Widget */
  extraView = XtCreateWidget("extraView", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(extraView);
  
  addQuickHelp(extraView);
  addHelpId(extraView,-1);

  
  /* ---------- Creating Widget:    defaultPublicDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "autoUnmanage", 0); n++;


  /* Creating Widget */
  defaultPublicDialog = XmCreateMessageDialog( parent, "defaultPublicDialog", args, n);
  
  addQuickHelp(defaultPublicDialog);
  addHelpId(defaultPublicDialog,40);

  
  /* ---------- Creating Widget:    rowColumn_4 -------------- */
  parent = defaultPublicDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 185); n++;
  XtSetArg(args[n], "width", 340); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  rowColumn_4 = XtCreateWidget("rowColumn_4", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_4);
  
  addQuickHelp(rowColumn_4);
  addHelpId(rowColumn_4,-1);

  
  /* ---------- Creating Widget:    scrolledWindow_2 -------------- */
  parent = rowColumn_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 143); n++;
  XtSetArg(args[n], "width", 334); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  scrolledWindow_2 = XtCreateWidget("scrolledWindow_2", xmScrolledWindowWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(scrolledWindow_2);
  
  addQuickHelp(scrolledWindow_2);
  addHelpId(scrolledWindow_2,-1);

  
  /* ---------- Creating Widget:    defaultPublicList -------------- */
  parent = scrolledWindow_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 143); n++;
  XtSetArg(args[n], "width", 334); n++;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "itemCount", 3); n++;
  XtSetArg(args[n], "items", RES_CONVERT("XmStringTable", "hallo, sss, ccc")); n++;
  XtSetArg(args[n], "selectionPolicy", 1); n++;
  XtSetArg(args[n], "visibleItemCount", 8); n++;


  /* Creating Widget */
  defaultPublicList = XtCreateWidget("defaultPublicList", xmListWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultPublicList);
  
  addQuickHelp(defaultPublicList);
  addHelpId(defaultPublicList,-1);

  
  /* ---------- Creating Widget:    defaultPublicText -------------- */
  parent = rowColumn_4;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "columns", 25); n++;


  /* Creating Widget */
  defaultPublicText = XtCreateWidget("defaultPublicText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultPublicText);
  
  addQuickHelp(defaultPublicText);
  addHelpId(defaultPublicText,-1);

  
  /* ---------- Creating Widget:    defaultPublicAdd -------------- */
  parent = defaultPublicDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  defaultPublicAdd = XtCreateWidget("defaultPublicAdd", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultPublicAdd);
  
  addQuickHelp(defaultPublicAdd);
  addHelpId(defaultPublicAdd,-1);

  
  /* ---------- Creating Widget:    defaultPublicDelete -------------- */
  parent = defaultPublicDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  defaultPublicDelete = XtCreateWidget("defaultPublicDelete", xmPushButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(defaultPublicDelete);
  
  addQuickHelp(defaultPublicDelete);
  addHelpId(defaultPublicDelete,-1);

  
  /* ---------- Creating Widget:    genericOptionsDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 280); n++;
  XtSetArg(args[n], "y", 50); n++;


  /* Creating Widget */
  genericOptionsDialog = XmCreateMessageDialog( parent, "genericOptionsDialog", args, n);
  
  addQuickHelp(genericOptionsDialog);
  addHelpId(genericOptionsDialog,41);

  
  /* ---------- Creating Widget:    xlCellTable_2 -------------- */
  parent = genericOptionsDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 30); n++;


  /* Creating Widget */
  xlCellTable_2 = XtCreateWidget("xlCellTable_2", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xlCellTable_2);
  
  addQuickHelp(xlCellTable_2);
  addHelpId(xlCellTable_2,-1);

  
  /* ---------- Creating Widget:    label_4 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4 = XtCreateWidget("label_4", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4);
  
  addQuickHelp(label_4);
  addHelpId(label_4,-1);

  
  /* ---------- Creating Widget:    label_4_1 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_1 = XtCreateWidget("label_4_1", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_1);
  
  addQuickHelp(label_4_1);
  addHelpId(label_4_1,-1);

  
  /* ---------- Creating Widget:    label_4_2 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 2); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_2 = XtCreateWidget("label_4_2", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_2);
  
  addQuickHelp(label_4_2);
  addHelpId(label_4_2,-1);

  
  /* ---------- Creating Widget:    label_4_3 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 3); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_3 = XtCreateWidget("label_4_3", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_3);
  
  addQuickHelp(label_4_3);
  addHelpId(label_4_3,-1);

  
  /* ---------- Creating Widget:    label_4_4 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 4); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_4 = XtCreateWidget("label_4_4", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_4);
  
  addQuickHelp(label_4_4);
  addHelpId(label_4_4,-1);

  
  /* ---------- Creating Widget:    genericQuickStartToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 130); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  genericQuickStartToggle = XtCreateWidget("genericQuickStartToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericQuickStartToggle);
  
  addQuickHelp(genericQuickStartToggle);
  addHelpId(genericQuickStartToggle,-1);

  
  /* ---------- Creating Widget:    genericAutoSaveToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 130); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 1); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  genericAutoSaveToggle = XtCreateWidget("genericAutoSaveToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericAutoSaveToggle);
  
  addQuickHelp(genericAutoSaveToggle);
  addHelpId(genericAutoSaveToggle,-1);

  
  /* ---------- Creating Widget:    genericAutoSaveScale -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 58); n++;
  XtSetArg(args[n], "width", 138); n++;
  XtSetArg(args[n], "x", 159); n++;
  XtSetArg(args[n], "y", 67); n++;
  XtSetArg(args[n], "decimalPoints", 1); n++;
  XtSetArg(args[n], "maximum", 100); n++;
  XtSetArg(args[n], "minimum", 1); n++;
  XtSetArg(args[n], "orientation", 2); n++;
  XtSetArg(args[n], "processingDirection", 3); n++;
  XtSetArg(args[n], "showValue", 1); n++;
  XtSetArg(args[n], "titleString", RES_CONVERT("XmString", "minutes")); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 2); n++;


  /* Creating Widget */
  genericAutoSaveScale = XtCreateWidget("genericAutoSaveScale", xmScaleWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericAutoSaveScale);
  
  addQuickHelp(genericAutoSaveScale);
  addHelpId(genericAutoSaveScale,-1);

  
  /* ---------- Creating Widget:    genericAutoSaveText -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 80); n++;
  XtSetArg(args[n], "y", 130); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 3); n++;


  /* Creating Widget */
  genericAutoSaveText = XtCreateWidget("genericAutoSaveText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericAutoSaveText);
  
  addQuickHelp(genericAutoSaveText);
  addHelpId(genericAutoSaveText,-1);

  
  /* ---------- Creating Widget:    genericMainTemplateText -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 80); n++;
  XtSetArg(args[n], "y", 130); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 4); n++;


  /* Creating Widget */
  genericMainTemplateText = XtCreateWidget("genericMainTemplateText", xmTextWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericMainTemplateText);
  
  addQuickHelp(genericMainTemplateText);
  addHelpId(genericMainTemplateText,-1);

  
  /* ---------- Creating Widget:    label_4_4_1 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 5); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_4_1 = XtCreateWidget("label_4_4_1", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_4_1);
  
  addQuickHelp(label_4_4_1);
  addHelpId(label_4_4_1,-1);

  
  /* ---------- Creating Widget:    quickHelpToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 130); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 5); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  quickHelpToggle = XtCreateWidget("quickHelpToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(quickHelpToggle);
  
  addQuickHelp(quickHelpToggle);
  addHelpId(quickHelpToggle,-1);

  
  /* ---------- Creating Widget:    label_4_4_1_1 -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 7); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;


  /* Creating Widget */
  label_4_4_1_1 = XtCreateWidget("label_4_4_1_1", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(label_4_4_1_1);
  
  addQuickHelp(label_4_4_1_1);
  addHelpId(label_4_4_1_1,-1);

  
  /* ---------- Creating Widget:    genericAfterLoadToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 130); n++;
  XtSetArg(args[n], "y", 10); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 7); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;


  /* Creating Widget */
  genericAfterLoadToggle = XtCreateWidget("genericAfterLoadToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(genericAfterLoadToggle);
  
  addQuickHelp(genericAfterLoadToggle);
  addHelpId(genericAfterLoadToggle,-1);

  
  /* ---------- Creating Widget:    toolTipLabel -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 70); n++;
  XtSetArg(args[n], "y", 230); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 6); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  toolTipLabel = XtCreateWidget("toolTipLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(toolTipLabel);
  
  addQuickHelp(toolTipLabel);
  addHelpId(toolTipLabel,-1);

  
  /* ---------- Creating Widget:    versionControlLabel -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 90); n++;
  XtSetArg(args[n], "y", 250); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 8); n++;
  XtSetArg(args[n], "horizontalAlignment", 2); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  versionControlLabel = XtCreateWidget("versionControlLabel", xmLabelWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(versionControlLabel);
  
  addQuickHelp(versionControlLabel);
  addHelpId(versionControlLabel,-1);

  
  /* ---------- Creating Widget:    toolTipToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 160); n++;
  XtSetArg(args[n], "y", 250); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Show")); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 6); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  toolTipToggle = XtCreateWidget("toolTipToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(toolTipToggle);
  
  addQuickHelp(toolTipToggle);
  addHelpId(toolTipToggle,-1);

  
  /* ---------- Creating Widget:    versionControlToggle -------------- */
  parent = xlCellTable_2;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 210); n++;
  XtSetArg(args[n], "y", 240); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 8); n++;
  XtSetArg(args[n], "horizontalAlignment", 0); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  versionControlToggle = XtCreateWidget("versionControlToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(versionControlToggle);
  
  addQuickHelp(versionControlToggle);
  addHelpId(versionControlToggle,-1);

  
  /* ---------- Creating Widget:    executeDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "autoUnmanage", 0); n++;
  XtSetArg(args[n], "cancelLabelString", RES_CONVERT("XmString", "Close")); n++;
  XtSetArg(args[n], "messageString", RES_CONVERT("XmString", "Do you want to execute one program.")); n++;
  XtSetArg(args[n], "okLabelString", RES_CONVERT("XmString", "Apply")); n++;
  XtSetArg(args[n], "resizePolicy", 1); n++;


  /* Creating Widget */
  executeDialog = XmCreateMessageDialog( parent, "executeDialog", args, n);
  
  addQuickHelp(executeDialog);
  addHelpId(executeDialog,20);

  
  /* ---------- Creating Widget:    executeCellTable -------------- */
  parent = executeDialog;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "resizeColumn", 0); n++;
  XtSetArg(args[n], "resizeRow", 0); n++;


  /* Creating Widget */
  executeCellTable = XtCreateWidget("executeCellTable", xlCellTableWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(executeCellTable);
  
  addQuickHelp(executeCellTable);
  addHelpId(executeCellTable,-1);

  
  /* ---------- Creating Widget:    executeRowColumn -------------- */
  parent = executeCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 119); n++;
  XtSetArg(args[n], "width", 107); n++;
  XtSetArg(args[n], "x", 5); n++;
  XtSetArg(args[n], "y", 5); n++;
  XtSetArg(args[n], "radioBehavior", 1); n++;
  XtSetArg(args[n], "cellColumn", 0); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 1); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  executeRowColumn = XtCreateWidget("executeRowColumn", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(executeRowColumn);
  
  addQuickHelp(executeRowColumn);
  addHelpId(executeRowColumn,-1);

  
  /* ---------- Creating Widget:    xmkmfToggleButton -------------- */
  parent = executeRowColumn;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  xmkmfToggleButton = XtCreateWidget("xmkmfToggleButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(xmkmfToggleButton);
  
  addQuickHelp(xmkmfToggleButton);
  addHelpId(xmkmfToggleButton,-1);

  
  /* ---------- Creating Widget:    makeDependToggleButton -------------- */
  parent = executeRowColumn;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  makeDependToggleButton = XtCreateWidget("makeDependToggleButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(makeDependToggleButton);
  
  addQuickHelp(makeDependToggleButton);
  addHelpId(makeDependToggleButton,-1);

  
  /* ---------- Creating Widget:    makeToggleButton -------------- */
  parent = executeRowColumn;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  makeToggleButton = XtCreateWidget("makeToggleButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(makeToggleButton);
  
  addQuickHelp(makeToggleButton);
  addHelpId(makeToggleButton,-1);

  
  /* ---------- Creating Widget:    execToggleButton -------------- */
  parent = executeRowColumn;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 0); n++;
  XtSetArg(args[n], "y", 0); n++;


  /* Creating Widget */
  execToggleButton = XtCreateWidget("execToggleButton", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(execToggleButton);
  
  addQuickHelp(execToggleButton);
  addHelpId(execToggleButton,-1);

  
  /* ---------- Creating Widget:    StopIcon -------------- */
  parent = executeCellTable;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "height", 60); n++;
  XtSetArg(args[n], "width", 60); n++;
  XtSetArg(args[n], "labelPixmap", createPixmapFromFile("Stop.xpm")); n++;
  XtSetArg(args[n], "labelType", 1); n++;
  XtSetArg(args[n], "pushButtonEnabled", 1); n++;
  XtSetArg(args[n], "recomputeSize", 0); n++;
  XtSetArg(args[n], "cellColumn", 1); n++;
  XtSetArg(args[n], "cellRow", 0); n++;
  XtSetArg(args[n], "horizontalAlignment", 1); n++;
  XtSetArg(args[n], "verticalAlignment", 1); n++;


  /* Creating Widget */
  StopIcon = XtCreateWidget("StopIcon", xmDrawnButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(StopIcon);
  
  addQuickHelp(StopIcon);
  addHelpId(StopIcon,-1);

  
  /* ---------- Creating Widget:    ProjectGenerateDialog -------------- */
  parent = XmForm_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "autoUnmanage", 0); n++;
  XtSetArg(args[n], "messageString", RES_CONVERT("XmString", "Overwrite the following files?")); n++;


  /* Creating Widget */
  ProjectGenerateDialog = XmCreateMessageDialog( parent, "ProjectGenerateDialog", args, n);
  
  addQuickHelp(ProjectGenerateDialog);
  addHelpId(ProjectGenerateDialog,17);

  
  /* ---------- Creating Widget:    rowColumn_1 -------------- */
  parent = ProjectGenerateDialog;

  /* Building resource list */
  n = 0;


  /* Creating Widget */
  rowColumn_1 = XtCreateWidget("rowColumn_1", xmRowColumnWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(rowColumn_1);
  
  addQuickHelp(rowColumn_1);
  addHelpId(rowColumn_1,-1);

  
  /* ---------- Creating Widget:    ImakefileGenerateToggle -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 50); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Imakefile")); n++;


  /* Creating Widget */
  ImakefileGenerateToggle = XtCreateWidget("ImakefileGenerateToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(ImakefileGenerateToggle);
  
  addQuickHelp(ImakefileGenerateToggle);
  addHelpId(ImakefileGenerateToggle,-1);

  
  /* ---------- Creating Widget:    MainGenerateToggle -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", 10); n++;
  XtSetArg(args[n], "y", 0); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Main Program")); n++;


  /* Creating Widget */
  MainGenerateToggle = XtCreateWidget("MainGenerateToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(MainGenerateToggle);
  
  addQuickHelp(MainGenerateToggle);
  addHelpId(MainGenerateToggle,-1);

  
  /* ---------- Creating Widget:    StubsGenerateToggle -------------- */
  parent = rowColumn_1;

  /* Building resource list */
  n = 0;
  XtSetArg(args[n], "x", -10); n++;
  XtSetArg(args[n], "y", 20); n++;
  XtSetArg(args[n], "labelString", RES_CONVERT("XmString", "Callback Stubs")); n++;


  /* Creating Widget */
  StubsGenerateToggle = XtCreateWidget("StubsGenerateToggle", xmToggleButtonWidgetClass,
			parent, args, n);
  
  /* Manage the Widget */
  XtManageChild(StubsGenerateToggle);
  
  addQuickHelp(StubsGenerateToggle);
  addHelpId(StubsGenerateToggle,-1);


}



// ----------------------------
// addCallbacks()
// ----------------------------

void MainWindow_base::addCallbacks()
{

  XtAddCallback( ProjectNew, "activateCallback",
                 manageWidget, (XtPointer)ProjectNewDialog); 

  XtAddCallback( FileOpen, "activateCallback",
                 manageWidget, (XtPointer)loadProjectDialog); 

  XtAddCallback( FileSaveAs, "activateCallback",
                 manageWidget, (XtPointer)saveProjectDialog); 

  XtAddCallback( newTemplates, "activateCallback",
                 do_newTemplates_CB, this);

  XtAddCallback( openTemplates, "activateCallback",
                 manageWidget, (XtPointer)openTemplatesDialog); 

  XtAddCallback( ProjectSettings, "activateCallback",
                 do_projectSettings_CB, this);

  XtAddCallback( ProjectLoadInterface, "activateCallback",
                 manageWidget, (XtPointer)loadInterfaceDialog); 

  XtAddCallback( ProjectShowHelp, "activateCallback",
                 showHelp_CB, this);

  XtAddCallback( ProjectResources, "activateCallback",
                 do_openGlobalResources_CB, this);

  XtAddCallback( ProjectGenerate, "activateCallback",
                 manageWidget, (XtPointer)ProjectGenerateDialog); 

  XtAddCallback( ProjectExecute, "activateCallback",
                 manageWidget, (XtPointer)executeDialog); 

  XtAddCallback( WidgetsPalette, "activateCallback",
                 showWidgetPalette_CB, this);

  XtAddCallback( OptionsMenu, "mapCallback",
                 quickHelpMenuMap_CB, this);

  XtAddCallback( genericOptionButton, "activateCallback",
                 manageWidget, (XtPointer)genericOptionsDialog); 

  XtAddCallback( OptionsGrid, "activateCallback",
                 manageWidget, (XtPointer)GridDialog); 

  XtAddCallback( UpdatePolicyMenu, "entryCallback",
                 updatePolicyChanged_CB, this);

  XtAddCallback( defaultShellMenu, "entryCallback",
                 defaultShellChanged_CB, this);

  XtAddCallback( viewDefaultsButton, "activateCallback",
                 manageWidget, (XtPointer)viewDefaultsDialog); 

  XtAddCallback( defaultPublicButton, "activateCallback",
                 manageWidget, (XtPointer)defaultPublicDialog); 

  XtAddCallback( HelpForHelp, "activateCallback",
                 DoHelpForHelp_CB, this);

  XtAddCallback( HelpOnContext, "activateCallback",
                 DoContextHelp_CB, this);

  XtAddCallback( HelpProduct, "activateCallback",
                 manageWidget, (XtPointer)ProductInfoDialog); 

  XtAddCallback( OpenIcon, "activateCallback",
                 manageWidget, (XtPointer)loadProjectDialog); 

  XtAddCallback( ProductIcon, "activateCallback",
                 manageWidget, (XtPointer)ProductInfoDialog); 

  XtAddCallback( WidgetPaletteIcon, "activateCallback",
                 showWidgetPalette_CB, this);

  XtAddCallback( TestModeToggle, "valueChangedCallback",
                 testModeChanged_CB, this);

  XtAddCallback( ProjectNewDialog, "mapCallback",
                 projectNewMapped_CB, this);

  XtAddCallback( ProjectNewOk, "activateCallback",
                 newProject_CB, this);

  XtAddCallback( ProjectNewCancel, "activateCallback",
                 newProjectCancel_CB, this);

  XtAddCallback( ProjectNewHelp, "activateCallback",
                 newProjectHelp_CB, this);

  XtAddCallback( ProjectSettingsDialog, "mapCallback",
                 projectSettingsMap_CB, this);

  XtAddCallback( PSOk, "activateCallback",
                 projectSettingsOk_CB, this);

  XtAddCallback( PSCancel, "activateCallback",
                 projectSettingsCancel_CB, this);

  XtAddCallback( PSHelp, "activateCallback",
                 projectSettingsHelp_CB, this);

  XtAddCallback( PSPaletteFiles, "activateCallback",
                 manageWidget, (XtPointer)PSPaletteFileSB); 

  XtAddCallback( PSSourceDirFiles, "activateCallback",
                 manageWidget, (XtPointer)PSSourceDirFileSB); 

  XtAddCallback( PSPaletteFileSB, "okCallback",
                 PSPaletteFileChanged_CB, this);

  XtAddCallback( PSSourceDirFileSB, "okCallback",
                 PSSourceDirChanged_CB, this);

  XtAddCallback( GridDialog, "mapCallback",
                 gridMap_CB, this);

  XtAddCallback( GridOk, "activateCallback",
                 gridOkCB_CB, this);

  XtAddCallback( GridCancel, "activateCallback",
                 gridCancelCB_CB, this);

  XtAddCallback( GridHelp, "activateCallback",
                 gridHelpCB_CB, this);

  XtAddCallback( saveProjectDialog, "okCallback",
                 saveProjectOk_CB, this);

  XtAddCallback( loadProjectDialog, "okCallback",
                 loadProjectOk_CB, this);

  XtAddCallback( loadInterfaceDialog, "okCallback",
                 loadInterfaceOk_CB, this);

  XtAddCallback( owOk, "activateCallback",
                 do_overwrite_CB, this);

  XtAddCallback( owCancel, "activateCallback",
                 overwriteCancel_CB, this);

  XtAddCallback( owHelp, "activateCallback",
                 overwriteHelp_CB, this);

  XtAddCallback( ExecIcon, "activateCallback",
                 manageWidget, (XtPointer)executeDialog); 

  XtAddCallback( openTemplatesDialog, "okCallback",
                 do_openTemplates_CB, this);

  XtAddCallback( quickStartDialog, "okCallback",
                 do_qsDo_CB, this);

  XtAddCallback( qsOpenButton, "valueChangedCallback",
                 do_qsChanged_CB, this);

  XtAddCallback( qsNewButton, "valueChangedCallback",
                 do_qsChanged_CB, this);

  XtAddCallback( qsRecoverButton, "valueChangedCallback",
                 do_qsChanged_CB, this);

  XtAddCallback( generateIcon, "activateCallback",
                 manageWidget, (XtPointer)ProjectGenerateDialog); 

  XtAddCallback( viewDefaultsDialog, "mapCallback",
                 do_viewDefaultsMap_CB, this);

  XtAddCallback( viewDefaultsDialog, "okCallback",
                 do_viewDefaultsOk_CB, this);

  XtAddCallback( defaultPublicDialog, "cancelCallback",
                 do_defaultPublicCancel_CB, this);

  XtAddCallback( defaultPublicDialog, "mapCallback",
                 do_defaultPublicMap_CB, this);

  XtAddCallback( defaultPublicDialog, "okCallback",
                 do_defaultPublicChanged_CB, this);

  XtAddCallback( defaultPublicList, "multipleSelectionCallback",
                 do_defaultPublicSelect_CB, this);

  XtAddCallback( defaultPublicText, "focusCallback",
                 do_defaultPublicFocus_CB, this);

  XtAddCallback( defaultPublicAdd, "activateCallback",
                 do_defaultPublicAdd_CB, this);

  XtAddCallback( defaultPublicDelete, "activateCallback",
                 do_defaultPublicDelete_CB, this);

  XtAddCallback( genericOptionsDialog, "mapCallback",
                 do_genericOptionsMap_CB, this);

  XtAddCallback( genericOptionsDialog, "okCallback",
                 do_genericOptionsOk_CB, this);

  XtAddCallback( executeDialog, "cancelCallback",
                 executeCancel_CB, this);

  XtAddCallback( executeDialog, "helpCallback",
                 executeHelp_CB, this);

  XtAddCallback( executeDialog, "okCallback",
                 executeOk_CB, this);

  XtAddCallback( StopIcon, "activateCallback",
                 do_stop_CB, this);

  XtAddCallback( ProjectGenerateDialog, "cancelCallback",
                 generateCancel_CB, this);

  XtAddCallback( ProjectGenerateDialog, "helpCallback",
                 generateHelp_CB, this);

  XtAddCallback( ProjectGenerateDialog, "okCallback",
                 generateOk_CB, this);

}



// ----------------------------
// addRelations()
// ----------------------------

void MainWindow_base::addRelations()
{
  XtVaSetValues(MenuBar, "menuHelpWidget", helpButton, NULL);

  XtVaSetValues(File, "subMenuId", FileMenu, NULL);

  XtVaSetValues(Project, "subMenuId", ProjectMenu, NULL);

  XtVaSetValues(Widgets, "subMenuId", WidgetsMenu, NULL);

  XtVaSetValues(ShellMenuButton, "subMenuId", ShellMenu, NULL);

  XtVaSetValues(ManagerMenuButton, "subMenuId", ManagerMenu, NULL);

  XtVaSetValues(PrimitivesMenuButton, "subMenuId", PrimitivesMenu, NULL);

  XtVaSetValues(DialogsMenuButton, "subMenuId", DialogsMenu, NULL);

  XtVaSetValues(Options, "subMenuId", OptionsMenu, NULL);

  XtVaSetValues(UpdatePolicy, "subMenuId", UpdatePolicyMenu, NULL);

  XtVaSetValues(defaultShellButton, "subMenuId", defaultShellMenu, NULL);

  XtVaSetValues(helpButton, "subMenuId", HelpMenu, NULL);

  XtVaSetValues(OpenIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(SaveIcon, "leftWidget", OpenIcon, NULL);

  XtVaSetValues(SaveIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(PaletteIcon, "leftWidget", SaveIcon, NULL);

  XtVaSetValues(PaletteIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(ProductIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(seperateBelowToolBar, "topWidget", ProductIcon, NULL);

  XtVaSetValues(WidgetPaletteIcon, "leftWidget", PaletteIcon, NULL);

  XtVaSetValues(WidgetPaletteIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(XmPanedWindow_1, "bottomWidget", quickHelp, NULL);

  XtVaSetValues(XmPanedWindow_1, "topWidget", seperateBelowToolBar, NULL);

  XtVaSetValues(MessageLabel, "bottomWidget", XmScrolledWindow_1, NULL);

  XtVaSetValues(XmScrolledWindow_1, "topWidget", MessageClearButton, NULL);

  XtVaSetValues(TestModeToggle, "bottomWidget", seperateBelowToolBar, NULL);

  XtVaSetValues(TestModeToggle, "rightWidget", ProductIcon, NULL);

  XtVaSetValues(TestModeToggle, "topWidget", MenuBar, NULL);

  XtVaSetValues(ProductInfoDialog, "cancelButton", PIClose, NULL);

  XtVaSetValues(label_2, "topWidget", ProductInfoLabel, NULL);

  XtVaSetValues(ProjectNewDialog, "cancelButton", ProjectNewCancel, NULL);

  XtVaSetValues(ProjectNewDialog, "defaultButton", ProjectNewOk, NULL);

  XtVaSetValues(ProjectNewDialog, "initialFocus", ProjectNewText, NULL);

  XtVaSetValues(ProjectNewText, "leftWidget", ProjectNewLabel, NULL);

  XtVaSetValues(ProjectNewOk, "topWidget", ProjectNewSeparator, NULL);

  XtVaSetValues(ProjectNewCancel, "topWidget", ProjectNewSeparator, NULL);

  XtVaSetValues(ProjectNewHelp, "topWidget", ProjectNewSeparator, NULL);

  XtVaSetValues(ProjectNewSeparator, "topWidget", ProjectNewLabel, NULL);

  XtVaSetValues(ProjectSettingsDialog, "cancelButton", PSCancel, NULL);

  XtVaSetValues(ProjectSettingsDialog, "defaultButton", PSOk, NULL);

  XtVaSetValues(ProjectSettingsDialog, "initialFocus", PSNameText, NULL);

  XtVaSetValues(PSNameText, "leftWidget", PSNameLabel, NULL);

  XtVaSetValues(PSPaletteLabel, "topWidget", PSNameLabel, NULL);

  XtVaSetValues(PSPaletteText, "leftWidget", PSPaletteLabel, NULL);

  XtVaSetValues(PSPaletteText, "rightWidget", PSPaletteFiles, NULL);

  XtVaSetValues(PSPaletteText, "topWidget", PSNameLabel, NULL);

  XtVaSetValues(PSLangOptionMenu, "topWidget", PSStartLabel, NULL);

  XtVaSetValues(XmSeparator_4, "bottomWidget", PSCancel, NULL);

  XtVaSetValues(XmSeparator_4, "topWidget", PSLangOptionMenu, NULL);

  XtVaSetValues(PSStartLabel, "topWidget", PSSourceDirLabel, NULL);

  XtVaSetValues(PCStartText, "leftWidget", PSStartLabel, NULL);

  XtVaSetValues(PCStartText, "topWidget", PSSourceDirLabel, NULL);

  XtVaSetValues(PSSourceDirLabel, "topWidget", PSPaletteLabel, NULL);

  XtVaSetValues(PSSourceDirText, "leftWidget", PSSourceDirLabel, NULL);

  XtVaSetValues(PSSourceDirText, "rightWidget", PSSourceDirFiles, NULL);

  XtVaSetValues(PSSourceDirText, "topWidget", PSPaletteLabel, NULL);

  XtVaSetValues(PSPaletteFiles, "topWidget", PSNameLabel, NULL);

  XtVaSetValues(PSSourceDirFiles, "topWidget", PSPaletteLabel, NULL);

  XtVaSetValues(GridDialog, "cancelButton", GridCancel, NULL);

  XtVaSetValues(GridDialog, "defaultButton", GridOk, NULL);

  XtVaSetValues(GridDialog, "initialFocus", GridRadioBox, NULL);

  XtVaSetValues(overwriteDialog, "cancelButton", owCancel, NULL);

  XtVaSetValues(overwriteDialog, "defaultButton", owOk, NULL);

  XtVaSetValues(scrolledWindow_1, "bottomWidget", owOk, NULL);

  XtVaSetValues(ExecIcon, "leftWidget", generateIcon, NULL);

  XtVaSetValues(ExecIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(generateIcon, "leftWidget", WidgetPaletteIcon, NULL);

  XtVaSetValues(generateIcon, "topWidget", MenuBar, NULL);

  XtVaSetValues(defaultViewOptionMenu, "subMenuId", defaultViewMenu, NULL);


}


// ----------------------------
// manageWidget()
// ----------------------------
void MainWindow_base::manageWidget(Widget, XtPointer c, XtPointer)
{
  XtManageChild((Widget)c);
}


// ----------------------------
// createPixmapFromData()
// ----------------------------

Pixmap MainWindow_base::createPixmapFromData(char **data)
{
  Pixmap pix, mask;
  XpmAttributes attr;

  memset(&attr, 0, sizeof(attr));
  attr.valuemask = XpmReturnPixels;

  if( !XpmCreatePixmapFromData(XtDisplay(LicView::getTopLevel()),
		               XRootWindowOfScreen(XtScreen(LicView::getTopLevel())),
			       data,
			       &pix, &mask, &attr) )
    return pix;
  return XmUNSPECIFIED_PIXMAP;
}

// ------------------------------------------
// createPixmapFromFile()
//
// Creating a pixmap by XpmRaedFileToPixmap()
// Uses XFILESEARCHPATH to find the pixmap
// default path: /usr/lib/X11/pixmaps/%N.xpm
// ------------------------------------------

Pixmap MainWindow_base::createPixmapFromFile(char *name)
{
  String string;
  Pixmap pix, mask;
  XpmAttributes attr;

  string = XtResolvePathname(XtDisplay(LicView::getTopLevel()),
			     "pixmaps", name, "", NULL,
			     NULL, 0, NULL);
  if(string) {
    memset(&attr, 0, sizeof(attr));
    attr.valuemask = XpmReturnPixels;
    if( !XpmReadFileToPixmap(XtDisplay(LicView::getTopLevel()),
			     XRootWindowOfScreen(
			     XtScreen(LicView::getTopLevel())),
			     string,
			     &pix, &mask, &attr) )
      return pix;
    return XmUNSPECIFIED_PIXMAP;
    XtFree(string);
  }
  return XmUNSPECIFIED_PIXMAP;
}

// ---------------------------------------------------------
// Following class method are used as a callback.
// The method is calling a abstract virtual method, which
// must be overloaded in the derived class 'MainWindow'
// ---------------------------------------------------------

void MainWindow_base::do_newTemplates_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_newTemplates(wg,callData);
}

void MainWindow_base::do_projectSettings_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_projectSettings(wg,callData);
}

void MainWindow_base::showHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->showHelp(wg,callData);
}

void MainWindow_base::do_openGlobalResources_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_openGlobalResources(wg,callData);
}

void MainWindow_base::showWidgetPalette_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->showWidgetPalette(wg,callData);
}

void MainWindow_base::quickHelpMenuMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->quickHelpMenuMap(wg,callData);
}

void MainWindow_base::updatePolicyChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->updatePolicyChanged(wg,callData);
}

void MainWindow_base::defaultShellChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->defaultShellChanged(wg,callData);
}

void MainWindow_base::DoHelpForHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->DoHelpForHelp(wg,callData);
}

void MainWindow_base::DoContextHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->DoContextHelp(wg,callData);
}

void MainWindow_base::testModeChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->testModeChanged(wg,callData);
}

void MainWindow_base::projectNewMapped_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->projectNewMapped(wg,callData);
}

void MainWindow_base::newProject_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->newProject(wg,callData);
}

void MainWindow_base::newProjectCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->newProjectCancel(wg,callData);
}

void MainWindow_base::newProjectHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->newProjectHelp(wg,callData);
}

void MainWindow_base::projectSettingsMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->projectSettingsMap(wg,callData);
}

void MainWindow_base::projectSettingsOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->projectSettingsOk(wg,callData);
}

void MainWindow_base::projectSettingsCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->projectSettingsCancel(wg,callData);
}

void MainWindow_base::projectSettingsHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->projectSettingsHelp(wg,callData);
}

void MainWindow_base::PSPaletteFileChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->PSPaletteFileChanged(wg,callData);
}

void MainWindow_base::PSSourceDirChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->PSSourceDirChanged(wg,callData);
}

void MainWindow_base::gridMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->gridMap(wg,callData);
}

void MainWindow_base::gridOkCB_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->gridOkCB(wg,callData);
}

void MainWindow_base::gridCancelCB_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->gridCancelCB(wg,callData);
}

void MainWindow_base::gridHelpCB_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->gridHelpCB(wg,callData);
}

void MainWindow_base::saveProjectOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->saveProjectOk(wg,callData);
}

void MainWindow_base::loadProjectOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->loadProjectOk(wg,callData);
}

void MainWindow_base::loadInterfaceOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->loadInterfaceOk(wg,callData);
}

void MainWindow_base::do_overwrite_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_overwrite(wg,callData);
}

void MainWindow_base::overwriteCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->overwriteCancel(wg,callData);
}

void MainWindow_base::overwriteHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->overwriteHelp(wg,callData);
}

void MainWindow_base::do_openTemplates_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_openTemplates(wg,callData);
}

void MainWindow_base::do_qsDo_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_qsDo(wg,callData);
}

void MainWindow_base::do_qsChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_qsChanged(wg,callData);
}

void MainWindow_base::do_viewDefaultsMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_viewDefaultsMap(wg,callData);
}

void MainWindow_base::do_viewDefaultsOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_viewDefaultsOk(wg,callData);
}

void MainWindow_base::do_defaultPublicCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicCancel(wg,callData);
}

void MainWindow_base::do_defaultPublicMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicMap(wg,callData);
}

void MainWindow_base::do_defaultPublicChanged_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicChanged(wg,callData);
}

void MainWindow_base::do_defaultPublicSelect_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicSelect(wg,callData);
}

void MainWindow_base::do_defaultPublicFocus_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicFocus(wg,callData);
}

void MainWindow_base::do_defaultPublicAdd_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicAdd(wg,callData);
}

void MainWindow_base::do_defaultPublicDelete_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_defaultPublicDelete(wg,callData);
}

void MainWindow_base::do_genericOptionsMap_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_genericOptionsMap(wg,callData);
}

void MainWindow_base::do_genericOptionsOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_genericOptionsOk(wg,callData);
}

void MainWindow_base::executeCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->executeCancel(wg,callData);
}

void MainWindow_base::executeHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->executeHelp(wg,callData);
}

void MainWindow_base::executeOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->executeOk(wg,callData);
}

void MainWindow_base::do_stop_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->do_stop(wg,callData);
}

void MainWindow_base::generateCancel_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->generateCancel(wg,callData);
}

void MainWindow_base::generateHelp_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->generateHelp(wg,callData);
}

void MainWindow_base::generateOk_CB(Widget wg, XtPointer clientData, XtPointer callData)
{
   ((MainWindow_base*)clientData)->generateOk(wg,callData);
}


